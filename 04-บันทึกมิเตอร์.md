# 04 — บันทึกมิเตอร์ (Meter Reading Management)

> เอกสารนี้เป็นสเปกฉบับสมบูรณ์ของฟีเจอร์ "บันทึกมิเตอร์" (`/meters`) ในระบบเดิม (PHP 5.6, Custom MVC ไม่มีเฟรมเวิร์ก) จัดทำขึ้นเพื่อใช้เป็นข้อมูลอ้างอิงในการเขียนระบบใหม่ด้วย PHP 8.4 โดยอ้างอิงจากซอร์สโค้ดจริงทั้งหมด ไม่มีการสมมติพฤติกรรมที่ไม่มีอยู่ในโค้ด
>
> ไฟล์ที่อ่านและใช้อ้างอิง:
> - `app/controllers/MeterManagementController.php`
> - `app/models/Meter.php`
> - `views/pages/meter-management/index.php`
> - `views/pages/meterExcel.php` (ไฟล์ที่ไม่ถูกอ้างอิงใช้งานจริง — ดูหมายเหตุด้านล่าง)
> - `views/pages/reports/meterExcel.php` (ไฟล์ export Excel ที่ใช้งานจริง)
> - `config/routes.php`
> - `app/core/Model.php` (base class)
> - `config/languages/th.php` (ป้ายข้อความ UI)

---

## 1. ภาพรวม

ฟีเจอร์นี้ใช้บันทึก **เลขมิเตอร์ไฟฟ้า/น้ำ** และ **ค่าใช้จ่ายอื่น ๆ (ค่าขยะ, ค่าส่วนกลาง)** ของแต่ละ "สินค้า" (ห้อง/ยูนิต, ระบุด้วย `pcode`) ต่อรอบเดือน/ปี เพื่อเป็นข้อมูลตั้งต้นสำหรับการออกใบแจ้งหนี้ (Invoice) ในภายหลัง

หน่วยงานที่ปรากฏในเมนูนี้ (ตาราง `ali_product`) ถูกจำกัดเฉพาะสินค้าที่อยู่ใน **หมวดหมู่ (category) id = 34 หรือ 54** และมี `sh = 1` (สถานะเปิดใช้งาน/แสดงผล) เท่านั้น — เงื่อนไขนี้ปรากฏซ้ำในทุก query ที่ดึงรายการสินค้า (`getAllMeters`, `searchMeter`, `countMeters`, ทั้งสองไฟล์ export Excel)

ความสามารถหลักของโมดูล:
1. **ตารางรายการมิเตอร์รายเดือน** — เลือกเดือน/ปี, ค้นหา, กรองสถานะ (บันทึกแล้ว/ยังไม่บันทึก), แสดงสถิติสรุป
2. **บันทึก/แก้ไขข้อมูลมิเตอร์รายห้อง (Modal)** — กรอกเลขมิเตอร์ไฟ/น้ำ วันที่อ่าน ค่าขยะ ค่าส่วนกลาง หมายเหตุ อัปโหลดรูปมิเตอร์ พร้อมรองรับกรณี "มิเตอร์รีเซ็ต" และ "เปลี่ยนมิเตอร์"
3. **นำเข้าข้อมูลจำนวนมากผ่านไฟล์ Excel** (Import) — อ่านไฟล์ฝั่ง client ด้วย SheetJS, ตรวจสอบ (validate) เทียบกับฐานข้อมูลก่อนบันทึกจริง (preview), แนบรูปมิเตอร์รายแถวได้, บันทึกแบบ bulk
4. **ส่งออกรายงาน Excel** (Export) — สร้างไฟล์ `.xlsx` ผ่าน PHPExcel โดยดึงข้อมูลตามช่วงเดือน/ปีและตัวกรองรหัสสินค้า
5. **ลบข้อมูลมิเตอร์** — ลบได้เฉพาะเมื่อยังไม่มีใบแจ้งหนี้ผูกอยู่

ระบบมี "จุดเริ่มต้นของระบบ" (system start) ตายตัวคือ **เดือน 9/ปี 2025 (กันยายน 2025)** — เดือน/ปีที่น้อยกว่านี้จะถูกบังคับให้สถิติเป็น 0 (แม้ข้อมูลดิบยังถูกดึงมาแสดงในตารางตามปกติ) ค่านี้ปรากฏ hard-coded ซ้ำหลายจุด (`$systemStartMonth = 9; $systemStartYear = 2025;` ใน controller, และ `$startYear = 2025` ใน view สำหรับ dropdown ปี)

โมเดล `Meter` (class `Meter extends Model`, `protected $table = 'me_meter';`) **ไม่ได้ใช้ CRUD มาตรฐานของ base `Model`** (`all()/find()/where()/create()/updateModel()/delete()/paginate()` ที่ใช้ primary key เดี่ยว `id`) เลยแม้แต่เมธอดเดียว เนื่องจากข้อมูลมิเตอร์ใช้ **composite key แบบ `pcode + month + year + meter_type`** โมเดลทั้งหมดเขียน raw SQL (`$this->db->prepare()/execute()`) เองทุกเมธอด — นี่คือจุดสำคัญที่ต้องออกแบบใหม่ในระบบ PHP 8.4 (ไม่มี primary key ตัวเดียวสำหรับ 1 แถวข้อมูลมิเตอร์ 1 ประเภทต่อ 1 ห้องต่อ 1 เดือน)

---

## 2. Routes / AJAX Endpoints ทั้งหมด

จาก `config/routes.php` (เฉพาะที่เกี่ยวกับ meter):

| Method | Path | Controller@Method | สถานะ |
|---|---|---|---|
| GET | `/meters` | `MeterManagementController@index` | ใช้งานจริง |
| GET | `/meters/edit/{id}` | `MeterManagementController@edit` | **route มีแต่ไม่มีเมธอด `edit()` ในคอนโทรลเลอร์ — dead route** |
| GET | `/meters/get-by-period` | `MeterManagementController@getByPeriod` | ใช้งานจริง |
| GET | `/meter-management/get-meter-images` | `MeterManagementController@getMeterImages` | ใช้งานจริง |
| GET | `/meter-management/get-meter-reset-changed` | `MeterManagementController@getMeterResetChanged` | ใช้งานจริง |
| GET | `/meters/check-invoice-exists` | `MeterManagementController@checkInvoiceExists` | ใช้งานจริง |
| GET | `/meter-management/get-current-costs` | `MeterManagementController@getCurrentCosts` | ใช้งานจริง |
| GET | `/meter-management/get-last-updated-info` | `MeterManagementController@getLastUpdatedInfo` | ใช้งานจริง |
| GET | `/meter-management/excel-preview-data` | `MeterManagementController@excelPreviewData` | ใช้งานจริง |
| POST | `/meters/store` | `MeterManagementController@store` | **route มีแต่ไม่มีเมธอด `store()` ในคอนโทรลเลอร์ — dead route** |
| POST | `/meters/update/{id}` | `MeterManagementController@update` | **route มีแต่ไม่มีเมธอด `update()` ในคอนโทรลเลอร์ — dead route** |
| POST | `/meter-management/save-meter` | `MeterManagementController@saveMeter` | ใช้งานจริง (บันทึกเดี่ยว) |
| POST | `/meter-management/bulk-save-from-excel` | `MeterManagementController@bulkSaveFromExcel` | ใช้งานจริง (บันทึกกลุ่มจาก Excel) |
| POST | `/meter-management/upload-temp-image` | `MeterManagementController@uploadTempImage` | ใช้งานจริง (อัปโหลดรูประหว่าง import) |
| POST | `/meters/delete` | `MeterManagementController@delete` | ใช้งานจริง |

**หมายเหตุสำคัญ:** คอนโทรลเลอร์ยังมีเมธอด `search()` (query สินค้าใน `ali_productcategory/ali_productgroup/ali_product` ด้วย keyword + filter `group_id`/`category_id`) แต่**ไม่มี route ใดชี้มาที่เมธอดนี้เลย** — เป็นโค้ดที่ตายแล้ว (dead code) ไม่ถูกเรียกใช้จาก UI ปัจจุบัน

การ **Export รายงาน Excel** ไม่ได้ผ่านระบบ router/`config/routes.php` — ฝั่ง JS (`reportExcel()`) ยิง `XMLHttpRequest` ตรงไปที่ path ไฟล์ PHP โดยตรง: `{origin}{basePath}/views/pages/reports/meterExcel.php?...` (ไฟล์ static ที่ execute ตรง ไม่ผ่าน controller/router)

### 2.1 `GET /meters` → `index()`

- ตรวจ `Auth::check()` ใน constructor ก่อนเสมอ (ถ้าไม่ login → redirect ไป `/login`)
- เรียก `$this->meterModel->getAllMeters()` **โดยไม่ส่ง month/year** → ใช้ค่าเริ่มต้นเป็นเดือน/ปีปัจจุบันของระบบ (`date('m')/date('Y')`) — เป็นเพียงการโหลดครั้งแรกเพื่อนับสถิติ `total`; ข้อมูลจริงในตารางจะถูกโหลดใหม่ทันทีฝั่ง client ผ่าน `loadMeters()` (เรียก `getByPeriod`) เมื่อ DOM พร้อม
- ส่ง `title`, `meters`, `stats` (มีแค่ `total`) ไปยัง view `pages/meter-management/index`

### 2.2 `GET /meters/get-by-period` → `getByPeriod()`

**Input (query string):** `month` (int, default = เดือนปัจจุบัน), `year` (int, default = ปีปัจจุบัน)

**Logic:**
1. ปฏิเสธถ้าไม่ใช่ GET (405)
2. คำนวณ `$isBeforeSystemStart` = จริงถ้า `year < 2025` หรือ (`year == 2025 AND month < 9`)
3. เรียก `$this->meterModel->getAllMeters($month, $year)` — ได้ array ของทุกห้อง พร้อมค่าที่คำนวณแล้ว (ดูหัวข้อ 7)
4. สำหรับแต่ละแถว query เพิ่มเติมทีละห้อง (`me_invoice WHERE pcode=? AND month=? AND year=?`) เพื่อเซ็ต `hasInvoice` (boolean) — ทำเป็น loop ต่อแถว (ไม่ batch), ถ้า query error จะ catch แล้วตั้ง `hasInvoice = false` เป็นค่า default
5. ถ้า `$isBeforeSystemStart == true` → **บังคับ overwrite ค่าตัวเลขทุกแถวเป็น 0**: `electricity, water, garbage, common_area, total` (เพื่อไม่ให้แสดงยอดเงินของช่วงก่อนเริ่มระบบ) — แต่ **ฟิลด์อื่นเช่นเลขมิเตอร์ (`meterelectricity`, `meterwater`), สถานะ, หมายเหตุ ฯลฯ ยังคงแสดงตามจริง**
6. คืนค่า JSON: `{ success: true, data: [...], count: N }` (ใช้ `JSON_UNESCAPED_UNICODE`)

### 2.3 `GET /meters/check-invoice-exists` → `checkInvoiceExists()`

**Input:** `pcode` (string, trim), `month` (int, 1–12), `year` (int, ≥2000)

**Validation:** ถ้า `pcode` ว่าง หรือ `month` ไม่อยู่ช่วง 1–12 หรือ `year < 2000` → throw Exception `'ข้อมูลไม่ครบถ้วน'`

**Query:** `SELECT COUNT(*) FROM me_invoice WHERE pcode=? AND month=? AND year=?`

**Return:** `{ success:true, hasInvoice: bool, count:int }` — ใช้ผลลัพธ์นี้เพื่อ**ตัดสินใจเปิด modal แก้ไขแบบ read-only หรือแก้ไขได้ปกติ** (ฝั่ง client เรียกก่อนเปิด modal ทุกครั้งที่กดปุ่มแก้ไข และก่อนกดลบ)

### 2.4 `GET /meter-management/get-meter-images` → `getMeterImages()`

**Input:** `pcode`, `month`, `year` (required, ทั้งหมด != 0/ว่าง)

**Logic:** เรียก private helper `getMeterImage($pcode,$month,$year,'ค่าไฟ')` และ `getMeterImage(...,'ค่าน้ำ')` แยกกัน — query `SELECT img FROM me_meter WHERE pcode=? AND month=? AND year=? AND meter_type=? AND img IS NOT NULL AND img != ''`

**Return:** `{ success:true, data:{ electricityImage, waterImage } }` (path หรือ `null`)

### 2.5 `GET /meter-management/get-meter-reset-changed` → `getMeterResetChanged()`

**Input:** `pcode`, `month`, `year` (required)

**Logic:** ดึง `meter_reset, meter_changed, old_meter_final, new_meter_start` จาก `me_meter` แยกสำหรับ `meter_type = 'ค่าไฟ'` และ `'ค่าน้ำ'`

**Return:** `{ success:true, data:{ electricity: {...}|null, water: {...}|null } }` — ใช้เติมค่า checkbox/ช่องกรอกเลขมิเตอร์เก่า-ใหม่ตอนเปิด modal แก้ไข

### 2.6 `GET /meter-management/get-current-costs` → `getCurrentCosts()`

**Input:** `pcode` (required)

**Logic:** `$this->meterModel->getCurrentCosts($pcode)` → `SELECT COALESCE(current_waste,0), COALESCE(current_central,0) FROM ali_product WHERE pcode=?`

**Return:** `{ success:true, data:{ current_waste, current_central } }` — ใช้เป็น**ค่าเริ่มต้นสำรอง**สำหรับค่าขยะ/ค่าส่วนกลาง เมื่อเปิด modal แก้ไขแล้วพบว่าทั้งสองค่าที่บันทึกไว้เป็น 0 (ดูหัวข้อ 6.2)

### 2.7 `GET /meter-management/get-last-updated-info` → `getLastUpdatedInfo()`

**Input:** `pcode`, `month` (≥1), `year` (≥2000)

**Logic:** `$this->meterModel->getLastUpdatedInfo(...)` — join `me_meter` กับ `me_users` (2 ครั้ง สำหรับ `updated_by`/`created_by`) เอาแถวที่ `GREATEST(updated_at, created_at)` ล่าสุด (`meter_type` ใดก็ได้ ไฟหรือน้ำ), เทียบ `updated_at` vs `created_at` เพื่อเลือกว่าจะแสดงชื่อผู้ "บันทึก" หรือ "แก้ไข" ล่าสุด (fallback ชื่อ = `'ระบบ'` ถ้าไม่พบชื่อผู้ใช้)

**Return:** `{ success:true, data: { updated_at, updated_by_name } | null }` — แสดงในส่วนท้าย modal เฉพาะเมื่อสถานะห้องนั้นเป็น `'saved'`

### 2.8 `GET /meter-management/excel-preview-data` → `excelPreviewData()`

**Input:** `month` (1–12), `year` (≥2000), `pcodes[]` (array of string, ต้องไม่ว่างหลัง trim/filter)

**Logic:** `$this->meterModel->getExcelPreviewBatch($pcodes, $month, $year)` (ดูรายละเอียด 6.4) — ดึงข้อมูลเดือนก่อนหน้า + ค่าเริ่มต้นค่าขยะ/ส่วนกลาง + สถานะใบแจ้งหนี้ + รูปเดิม เป็น batch ต่อ pcode ที่ขอมา ใช้สำหรับหน้าจอ preview ก่อนนำเข้าไฟล์ Excel

**Return:** `{ success:true, data: { [pcode]: {...} } }`

### 2.9 `POST /meter-management/save-meter` → `saveMeter()` (บันทึกเดี่ยว)

ดูรายละเอียดครบถ้วนใน **หัวข้อ 6.1**

### 2.10 `POST /meter-management/bulk-save-from-excel` → `bulkSaveFromExcel()` (บันทึกกลุ่มจาก Excel)

ดูรายละเอียดครบถ้วนใน **หัวข้อ 6.4**

### 2.11 `POST /meter-management/upload-temp-image` → `uploadTempImage()`

**Input (multipart/form-data):** `img_file` (file), `pcode` (required), `month` (default = เดือนปัจจุบัน), `year` (default = ปีปัจจุบัน), `meter_type` (default = `'electricity'`)

**Logic:** เรียก private `saveMeterImage('img_file', $pcode, $month, $year, $meterType)` (ตัวเดียวกับที่ `saveMeter()` ใช้ — ดูกฎการอัปโหลดในหัวข้อ 6.5) — ใช้ตอนแนบรูปมิเตอร์ต่อแถวระหว่างขั้นตอน preview ของการ import Excel (อัปโหลดทันทีเมื่อผู้ใช้เลือกไฟล์ในตาราง preview ก่อนกด "บันทึก")

**Return:** สำเร็จ → `{ success:true, path }`; ล้มเหลว → `{ success:false, message:'บันทึกรูปไม่สำเร็จ กรุณาตรวจสอบประเภทไฟล์ (JPG/PNG/GIF) และขนาด (ไม่เกิน 5MB)' }`

### 2.12 `POST /meters/delete` → `delete()`

ดูรายละเอียดครบถ้วนใน **หัวข้อ 6.3**

---

## 3. โครงสร้างข้อมูล / ฟิลด์ในตาราง

โปรเจกต์นี้ไม่มีไฟล์ schema SQL แนบมาด้วย โครงสร้างตารางด้านล่างสรุปมาจาก column ที่ปรากฏจริงในทุก query ของโมดูลนี้

### 3.1 `me_meter` (ตารางหลักของเลขมิเตอร์ — 1 แถวต่อ 1 ประเภทมิเตอร์ ต่อ 1 ห้อง ต่อ 1 เดือน/ปี)

| Field | ชนิด (สันนิษฐาน) | ความหมาย |
|---|---|---|
| `meter_id` | UUID/PK | สร้างอัตโนมัติผ่าน DB trigger (โค้ด insert ไม่ระบุค่านี้เลย — คอมเมนต์ยืนยันชัดเจน) |
| `meter_type` | varchar | `'ค่าไฟ'` หรือ `'ค่าน้ำ'` เท่านั้น (ค่าขยะ/ส่วนกลางอยู่คนละตาราง) |
| `pcode` | varchar | รหัสห้อง/สินค้า อ้างอิง `ali_product.pcode` |
| `month` | int | 1–12 |
| `year` | int | ค.ศ. |
| `reading_value` | int/decimal, **NULL ได้** | เลขมิเตอร์ที่อ่านได้ในรอบนี้ |
| `remark` | text, nullable | หมายเหตุ (มักเก็บช่วงวันที่จดแบบข้อความไทย) |
| `img` | varchar, nullable | path รูปภาพ เช่น `/uploads/meters/xxx.jpg` |
| `reading_date` | date, nullable | วันที่อ่านมิเตอร์ (ดูกฎการคำนวณอัตโนมัติในหัวข้อ 6.1.3) |
| `meter_reset` | tinyint (0/1) | flag มิเตอร์ถูกรีเซ็ต/หมุนครบรอบ |
| `meter_changed` | tinyint (0/1) | flag เปลี่ยนตัวมิเตอร์ใหม่/มิเตอร์เสีย |
| `old_meter_final` | int, nullable | เลขมิเตอร์เก่าตัวสุดท้ายก่อนเปลี่ยน (ใช้เฉพาะกรณี `meter_changed=1`) |
| `new_meter_start` | int, nullable | เลขมิเตอร์ใหม่เริ่มต้น (ใช้เฉพาะกรณี `meter_changed=1`) |
| `created_at`, `created_by` | datetime, FK→me_users.id | |
| `updated_at`, `updated_by` | datetime, FK→me_users.id | |

**Unique key เชิงตรรกะ:** `(pcode, month, year, meter_type)` — ใช้เช็คซ้ำผ่าน `meterRecordExists()` ก่อน insert/update ทุกครั้ง (ไม่ใช่ SQL `UNIQUE` constraint ระดับ DB ที่เห็นในโค้ด แต่บังคับด้วย application logic)

### 3.2 `me_meter_ohter` (ค่าใช้จ่ายอื่น ๆ — ค่าขยะ, ค่าส่วนกลาง)

| Field | ความหมาย |
|---|---|
| `meter_ohter_id` | PK, สร้างผ่าน trigger |
| `meter_ohter_type` | `'ค่าขยะ'` หรือ `'ค่าส่วนกลาง'` |
| `pcode`, `month`, `year` | เหมือนด้านบน |
| `price` | decimal, **NULL ได้** |
| `remark` | text, nullable (คัดลอกหมายเหตุเดียวกับฟอร์มหลัก) |
| `created_at/by`, `updated_at/by` | เหมือนด้านบน |

**Unique key เชิงตรรกะ:** `(pcode, month, year, meter_ohter_type)`

### 3.3 `me_invoice` (ใช้แค่เช็คว่ามีใบแจ้งหนี้ผูกกับงวดนี้หรือยัง — ตารางหลักอยู่นอกสโคปเอกสารนี้)

Field ที่โมดูลนี้อ้างถึง: `pcode`, `month`, `year` (สำหรับ `COUNT(*)`) — เอกสารนี้ไม่ deep-dive โครงสร้างเต็มของ Invoice

### 3.4 `ali_product` (สินค้า/ห้อง — ต้นทาง `pcode`, ราคาต่อหน่วย, ค่า default)

Field ที่โมดูลนี้อ้างถึง: `pcode`, `pdesc`, `group_id`, `sh` (1=แสดง), `wh_st` (0/null=ว่าง, 1=ไม่ว่าง, 2=จอง, 3=โครงการในอนาคต), `sales_rep_code`, `meter_1_ppu` (ราคาไฟต่อหน่วย), `meter_0_ppu` (ราคาน้ำต่อหน่วย), `meter_1_latest`/`meter_0_latest` (แคชเลขมิเตอร์ล่าสุด — อัปเดตทุกครั้งที่บันทึกมิเตอร์), `current_waste`/`current_central` (ค่าขยะ/ส่วนกลางเริ่มต้นล่าสุดที่เคยบันทึก ใช้เติมอัตโนมัติ)

### 3.5 `ali_productcategory` / `ali_productgroup`

ใช้ join เพื่อกรองเฉพาะหมวดหมู่ `id = 34 หรือ 54` และดึงชื่อกลุ่ม (`groupname`) — join แบบ 2 ทาง (`pg1` ผ่าน `id_cate`, `pg2` ผ่าน `group_id`) แล้วใช้ `COALESCE(pg1.groupname, pg2.groupname)`

### 3.6 `me_users`

ใช้ join เพื่อดึงชื่อผู้บันทึก/แก้ไขล่าสุด (field: `id`, `name`)

---

## 4. หน้าจอและองค์ประกอบ UI

ไฟล์: `views/pages/meter-management/index.php` (3,074 บรรทัด)

### 4.1 การ์ดสถิติ (Stats cards) — คำนวณฝั่ง client ทั้งหมดจาก `allMeters` (array ที่โหลดมาจาก `getByPeriod`)

`totalMeters`, `savedCount`, `unsavedCount`, `totalElectricity`, `totalWater`, `totalGarbage`, `totalCommonArea`, `grandTotal` — คำนวณใหม่ทุกครั้งที่ `filterTable()` ถูกเรียก (คือกรองจาก `allMeters` ไม่ยิง AJAX ซ้ำ) `status` ถือว่า "saved" ถ้าค่าเท่ากับ `'saved'`, `true`, `1`, หรือ `'1'` (รองรับหลายฟอร์แมต)

### 4.2 แถบตัวกรอง (Filter bar)

- ช่องค้นหา `#searchUser` — placeholder = "รหัสสินค้า" ค้นหาแบบ client-side ทันที (`input` event + Enter key) เทียบ `pcode`/`pdesc` (lowercase, `includes()`) กับ `allMeters` ที่โหลดมาแล้ว **ไม่ยิง AJAX ใหม่**
- ปุ่มล้างค้นหา `#resetSearch`
- ปุ่ม "Excel" (`#btnReportExcel`) → เปิด modal กรองก่อน export (`openExcelFilterModal()`)
- ปุ่ม "อัพโหลด" (`#btnImportExcel`, สไตล์ gradient เขียวเคลื่อนไหว) → เปิด modal นำเข้า (`openImportExcelModal()`)
- Dropdown เดือน `#filterMonth` (ค่าเริ่มต้น = เดือนปัจจุบัน หรือ `09` ถ้าปัจจุบัน < กันยายน 2025), ปี `#filterYear` (ตัวเลือกเริ่มจากปี **2025** ถึงปีปัจจุบัน) — `change` event → เรียก `loadMeters()` (ยิง AJAX ใหม่ทุกครั้ง)
- Dropdown สถานะ `#filterStatus` (ทั้งหมด/บันทึกแล้ว/ยังไม่บันทึก) → `change` event → กรอง client-side (`filterTable()`)

### 4.3 ตารางรายการ (`#MetersTable`)

คอลัมน์: ลำดับ, รหัสสินค้า, เดือน/ปี (ซ่อนบนจอเล็ก), สถานะ, มิเตอร์ไฟฟ้า (เดือนก่อน→ปัจจุบัน), มิเตอร์น้ำ (เดือนก่อน→ปัจจุบัน), ค่าไฟ, ค่าน้ำ, ค่าขยะ, ค่าส่วนกลาง, รวม, การจัดการ (ปุ่มแก้ไข/ลบ)

- **ปุ่มแก้ไข**: ไอคอนและ title เปลี่ยนตาม `hasInvoice` — ถ้ามีใบแจ้งหนี้แล้วแสดงไอคอนตา (`bi-eye`, title="ดูข้อมูล") แทนดินสอ (`bi-pencil`, title="แก้ไข") — คลิกแล้วยิง `checkInvoiceExists` ก่อนเปิด modal เสมอ (ไม่เชื่อ flag ที่ cache มาจากตารางเพียงอย่างเดียว) แล้วเรียก `openEditMeterModal(meter, hasInvoice)`
- **ปุ่มลบ**: ปรากฏเฉพาะแถวสถานะ `saved` และเป็นปุ่ม enable; แถว `unsaved` จะแสดงปุ่มที่ `disabled` (ไอคอน `bi-slash-circle`)

### 4.4 Modal แก้ไขข้อมูลมิเตอร์ (`#editMeterModal`, ขนาด `modal-xl`)

แบ่งเป็น 4 การ์ดย่อยในฟอร์ม `#editMeterForm`:

1. **ข้อมูลทั่วไป** (readonly ทั้งหมด): รหัสสินค้า, เดือน, ปี, ราคาไฟ/หน่วย (`electricity_ppu`), ราคาน้ำ/หน่วย (`water_ppu`)
2. **มิเตอร์ไฟ** (การ์ดขอบสีน้ำเงิน): วันที่อ่าน (`dateMeterElectricity`), เลขมิเตอร์เดือนก่อน (readonly, `previousMeterElectricity`), เลขมิเตอร์ปัจจุบัน (`readingValueElectricity`, รับเฉพาะตัวเลขผ่าน `chkNumber()`), checkbox "รีเซ็ต" (`electricityMeterReset`) และ "เปลี่ยนมิเตอร์/เสีย" (`electricityMeterChanged`) — เมื่อติ๊ก "เปลี่ยนมิเตอร์" จะเปิดส่วน `#electricityMeterChangeSection` ให้กรอก "มิเตอร์เก่าสุดท้าย" (`electricityOldMeterFinal`) และ "มิเตอร์ใหม่เริ่มต้น" (`electricityNewMeterStart`)
3. **มิเตอร์น้ำ** (การ์ดขอบสีฟ้า): โครงสร้างเหมือนข้อ 2 ทุกประการ (คนละ id เท่านั้น)
4. **ค่าใช้จ่ายอื่น ๆ**: ค่าขยะ (`readingValueGarbage`), ค่าส่วนกลาง (`readingValueCommonArea`)
5. **รูปภาพและหมายเหตุ**: ช่องอัปโหลดรูปไฟ/น้ำ (พร้อม preview + ปุ่มลบรูป), ปุ่ม "เติมวันที่จด" (`autoFillRemark()`), textarea หมายเหตุ (`meterRemark`)

**Footer:** พื้นที่แสดง "บันทึกล่าสุดโดยใคร/เมื่อไหร่" (`#lastUpdatedInfo`), ปุ่มยกเลิก, ปุ่มบันทึก (`#saveMeterChanges`)

**พฤติกรรม checkbox รีเซ็ต ↔ เปลี่ยนมิเตอร์ (ทั้งไฟและน้ำ, ผูก event แยกกันแต่ logic เหมือนกัน):**
- ติ๊ก "เปลี่ยนมิเตอร์" → ปลดติ๊ก "รีเซ็ต" อัตโนมัติ, แสดง section กรอกเลขเก่า/ใหม่
- ยกเลิกติ๊ก "เปลี่ยนมิเตอร์" → ซ่อน section, **เคลียร์ค่าที่กรอกไว้ในช่องเก่า/ใหม่ทิ้งทันที**
- ติ๊ก "รีเซ็ต" → ปลดติ๊ก "เปลี่ยนมิเตอร์" อัตโนมัติ, ซ่อน section เปลี่ยนมิเตอร์, เคลียร์ค่าเก่า/ใหม่ทิ้ง
- **ทั้งสอง checkbox แยกกันไม่ได้ผูก `mutually exclusive` ด้วย `radio`/`disable` แต่ใช้ JS uncheck คู่แข่งเอง** ดังนั้นทางเทคนิคยังสามารถ manipulate DOM หรือส่ง POST ตรงให้ทั้งสอง flag เป็น 1 พร้อมกันได้ (ดูหัวข้อ 9 เรื่องลำดับความสำคัญที่ backend ใช้)

**พฤติกรรม read-only mode (เมื่อ `hasInvoice = true`):**
- Title ของ modal เปลี่ยนเป็น "ดูข้อมูลมิเตอร์" แทน "ข้อมูลมิเตอร์"
- ฟิลด์ที่แก้ไขได้ทั้งหมด (`readingValueElectricity/Water/Garbage/CommonArea`, `meterRemark`, `dateMeterElectricity/Water`) ถูกใส่ `readonly` + `disabled`
- checkbox รีเซ็ต/เปลี่ยนมิเตอร์ และช่องเลขเก่า/ใหม่ ถูก `disabled`
- ช่องอัปโหลดรูป (`img_electricity`, `img_water`) ถูก `disabled`, ปุ่มลบรูปถูกซ่อน
- ปุ่ม "บันทึก" และปุ่ม "เติมวันที่จด" ถูกซ่อน (`display:none`)

**Auto-fill ค่าขยะ/ค่าส่วนกลาง (เฉพาะโหมดแก้ไข ไม่ใช่ read-only):** ถ้าทั้งค่าขยะและค่าส่วนกลางที่บันทึกไว้ของงวดนี้เป็น 0 ทั้งคู่ → ยิง `getCurrentCosts` เพื่อดึงค่าล่าสุดจาก `ali_product.current_waste/current_central` มาเติมแทน (เฉพาะฟิลด์ที่ค่าจาก DB > 0)

**Auto-fill หมายเหตุ (`generateRemarkText()`):** ถ้าหมายเหตุว่างอยู่ตอนเปิด modal (และไม่ใช่ read-only) จะสร้างข้อความอัตโนมัติรูปแบบ:
- ถ้าวันที่อ่านไฟ = วันที่อ่านน้ำ และวันเริ่มต้น (เดือนก่อน+1วัน) ของทั้งสองตรงกัน → `"วันที่จด {เริ่ม} ถึง {จบ}"` (บรรทัดเดียว)
- ไม่ตรงกัน → แยกเป็น 2 บรรทัด `"จดไฟ: {เริ่ม} ถึง {จบ}"` และ `"จดน้ำ: {เริ่ม} ถึง {จบ}"`
- วันที่ "เริ่ม" = วันที่อ่านมิเตอร์ของเดือนก่อนหน้า **+ 1 วัน** (ดึงจาก `dateMeterElectricityBefore`/`dateMeterWaterBefore` ที่ backend ส่งมา) ถ้าไม่มีวันที่จดเดือนก่อน จะแสดง `"ไม่มีจด"` แทน
- แปลงวันเป็นรูปแบบไทยเต็ม เช่น `16 กรกฎาคม 2569` (ปี พ.ศ. = ค.ศ.+543)
- มีปุ่ม "เติมวันที่จด" (`#btnAutoFillRemark`) ให้กดเติมซ้ำเองได้ทุกเมื่อ และมี **event แบบ real-time**: ทุกครั้งที่ผู้ใช้เปลี่ยนค่าวันที่อ่านไฟ/น้ำ (ผ่าน `change` event แบบ delegated บน `document`) หมายเหตุจะถูก generate ใหม่ทับค่าเดิมโดยอัตโนมัติ (ถ้าฟิลด์หมายเหตุไม่ได้ disabled) — **หมายความว่าแม้ผู้ใช้เคยแก้หมายเหตุเองแล้ว การเปลี่ยนวันที่จะเขียนทับข้อความที่แก้ไว้เอง**

### 4.5 Modal Popup รูปภาพเต็มจอ (`#imageModal`)

คลิกรูป preview ใด ๆ (`onclick="openImageModal(src)"`) จะเปิด modal เต็มจอสีดำโปร่งแสง คลิกพื้นหลัง (นอกรูป) เพื่อปิด

### 4.6 Modal กรองก่อน Export Excel (`#excelFilterModal`)

ฟิลด์: รหัสสินค้า/รายละเอียด (`excelProductCode`, ค้นหาแบบ contains, เว้นว่าง = ทั้งหมด), ช่วงเริ่มต้น (เดือน+ปี), ช่วงสิ้นสุด (เดือน+ปี) — ปีเริ่มต้น dropdown จาก 2025

**Validation ก่อนส่ง:** `startDate > endDate` (เทียบเป็น `Date` object ของเดือน 1 วันที่ 1) → แจ้งเตือน "วันที่เริ่มต้นต้องน้อยกว่าหรือเท่ากับวันที่สิ้นสุด" และไม่ export

### 4.7 Modal นำเข้า Excel (`#importExcelModal`, full-screen)

มี 3 ขั้นตอนแสดงผลด้วย step indicator (badge active/done/idle):
1. **เลือกไฟล์** — เลือกเดือน/ปีที่จะบันทึก (sync มาจากตัวกรองหลักตอนเปิด modal), drag-and-drop หรือคลิกเลือกไฟล์ `.xlsx`/`.xls` (สูงสุด "ไม่เกิน 10MB" ตามข้อความ UI — **ไม่มีการ validate ขนาดไฟล์จริงในโค้ด JS ทั้งฝั่ง drop และฝั่ง input change เป็นเพียงข้อความแจ้งผู้ใช้**), ตรวจเฉพาะนามสกุลไฟล์ (`xlsx`/`xls`) เท่านั้น
2. **ตรวจสอบ (Preview)** — ตารางแสดงทุกแถวจากไฟล์ พร้อม badge สถานะ ok/error, การ์ดสรุป (แถวทั้งหมด/พร้อมบันทึก/มีปัญหา), ช่องแนบรูปมิเตอร์ไฟ/น้ำต่อแถว (อัปโหลดทันทีเมื่อเลือกไฟล์ ผ่าน `uploadTempImage`)
3. **บันทึก** — ปุ่ม "บันทึก N รายการ" ปรากฏเมื่อมีอย่างน้อย 1 แถวผ่านการตรวจสอบ

รูปแบบคอลัมน์ในไฟล์ Excel (แสดงเป็น badge ในหน้า UI):

| คอลัมน์ | ความหมาย |
|---|---|
| A | รหัสสินค้า |
| B | วันอ่านไฟ (รูปแบบ `YYYY-MM-DD`) |
| C | มิเตอร์ไฟ (จำนวนเต็ม) |
| D | วันอ่านน้ำ (รูปแบบ `YYYY-MM-DD`) |
| E | มิเตอร์น้ำ (จำนวนเต็ม) |

ข้อมูลเริ่มอ่านจาก **แถวที่ 2** เป็นต้นไป (แถว 1 = หัวตาราง ถูกข้ามเสมอโดยโค้ดเริ่ม loop จาก index 1)

รายละเอียด validation/flow เต็มอยู่ใน **หัวข้อ 6.4**

---

## 5. Input ทั้งหมดพร้อมกฎ Validation

### 5.1 ฟอร์มบันทึกเดี่ยว (`saveMeter`)

| ฟิลด์ POST | Type ที่แปลง | Required? | กฎ |
|---|---|---|---|
| `pcode` | string (trim) | **ใช่** | ว่างไม่ได้ |
| `month` | int | **ใช่** | ต้อง != 0 (ไม่ validate ช่วง 1-12 ที่ backend ของ endpoint นี้โดยตรง — พึ่งพา dropdown ฝั่ง UI) |
| `year` | int | **ใช่** | ต้อง != 0 |
| `electricity` | int หรือ `null` | ไม่บังคับ | ค่าว่าง (`''`) → เก็บเป็น `null` ไม่ใช่ 0 |
| `water` | int หรือ `null` | ไม่บังคับ | เหมือน electricity |
| `garbage` | float หรือ `null` | ไม่บังคับ | เหมือนกัน |
| `common_area` | float หรือ `null` | ไม่บังคับ | เหมือนกัน |
| `remark` | string (trim) | ไม่บังคับ | ไม่จำกัดความยาว |
| `dateMeterElectricity` | date string หรือ `null` | ไม่บังคับ | ค่าว่าง → `null` |
| `dateMeterWater` | date string หรือ `null` | ไม่บังคับ | ค่าว่าง → `null` |
| `electricity_meter_reset` | int (0/1) | ไม่บังคับ | default 0 |
| `water_meter_reset` | int (0/1) | ไม่บังคับ | default 0 |
| `electricity_meter_changed` | int (0/1) | ไม่บังคับ | default 0 |
| `water_meter_changed` | int (0/1) | ไม่บังคับ | default 0 |
| `electricity_old_meter_final` | int หรือ `null` | บังคับที่ **ฝั่ง JS เท่านั้น** เมื่อ `electricity_meter_changed=1` | backend ไม่ enforce ซ้ำ |
| `electricity_new_meter_start` | int หรือ `null` | เช่นเดียวกัน | เช่นเดียวกัน |
| `water_old_meter_final` / `water_new_meter_start` | เช่นเดียวกัน | เช่นเดียวกัน | เช่นเดียวกัน |
| `current_electricity_image` / `current_water_image` | string (hidden field) | ไม่บังคับ | path รูปเดิมที่จะคงไว้ ถ้าไม่มีไฟล์ใหม่อัปโหลด |
| `img_electricity` / `img_water` | file (multipart) | ไม่บังคับ | ดูกฎอัปโหลดหัวข้อ 6.5 |

**Validation ฝั่ง client ก่อนส่ง (JS ใน `saveMeterChanges` click handler):**
1. `pcode`/`month`/`year` ต้องไม่ว่าง → ถ้าว่างแสดง SweetAlert "ข้อมูลไม่ครบถ้วน"
2. ถ้า `electricityMeterChanged` ติ๊กอยู่ แต่ `electricityOldMeterFinal` หรือ `electricityNewMeterStart` ว่าง → แจ้งเตือน "กรุณาระบุเลขมิเตอร์ไฟเก่าสุดท้ายและมิเตอร์ใหม่เริ่มต้น" และหยุดการส่ง
3. เช่นเดียวกันสำหรับน้ำ: "กรุณาระบุเลขมิเตอร์น้ำเก่าสุดท้ายและมิเตอร์ใหม่เริ่มต้น"
4. มี dialog ยืนยันก่อนส่งจริงเสมอ (SweetAlert confirm)

**Validation ฝั่ง backend (controller):** ตรวจแค่ `pcode`/`month`/`year` เท่านั้น (ดังตารางข้างบน) — **ไม่มีการตรวจซ้ำเรื่องเลขมิเตอร์เก่า/ใหม่เมื่อ `meter_changed=1`, ไม่ตรวจว่าเลขมิเตอร์ปัจจุบันต้องมากกว่าหรือเท่ากับเดือนก่อน, ไม่ตรวจค่าติดลบของ `electricity`/`water`/`garbage`/`common_area`** (การกรอกเป็น `(int)`/`(float)` cast เฉย ๆ) การป้องกันค่าติดลบเกิดขึ้นเฉพาะตอน**คำนวณหน่วยใช้ (units)** ในโมเดล ไม่ใช่ตอน validate การบันทึก raw value

### 5.2 ตัวเลข "จำนวนเต็มเท่านั้น" (`chkNumber(ele)`)

ฟิลด์เลขมิเตอร์ไฟ/น้ำ (`readingValueElectricity`, `readingValueWater`) ผูก `OnKeyPress="return chkNumber(this)"` — อนุญาตเฉพาะตัวอักษร `'0'`–`'9'` (เทียบ `String.fromCharCode(event.keyCode)`) แต่ **ฟังก์ชันนี้ไม่ `return false` จริง ๆ อย่างสมบูรณ์ในทุก branch** (มีแค่ `if (vchar<'0' || vchar>'9') return false;` ตามด้วยบรรทัดไร้ผล `ele.onKeyPress=vchar;`) — เป็นการป้องกันแบบ soft ที่ยังหลีกเลี่ยงได้ด้วยการ paste ข้อความ (ไม่มี `input`/`paste` event handler ป้องกันเพิ่ม)

### 5.3 ฟอร์มนำเข้า Excel (ต่อแถว, ตรวจฝั่ง client ด้วย `buildImportPreviewTable()`)

| เงื่อนไข | ข้อความ error (ถ้าไม่ผ่าน) |
|---|---|
| `pcode` ต้องมีอยู่จริงใน `ali_product` (query `getExcelPreviewBatch` คืน `exists=true`, `sh=1`) | "รหัสสินค้าต้องมีในระบบเท่านั้น" |
| มิเตอร์ไฟ (`meter_elec`) ต้องเป็นจำนวนเต็ม (regex `^-?\d+$` หลังตัด comma) | "มิเตอร์ไฟต้องเป็นจำนวนเต็มเท่านั้น" |
| ถ้ามีค่ามิเตอร์ไฟเดือนก่อนหน้า (`prev_elec`) และ `prev_elec > 0` → มิเตอร์ไฟที่กรอกต้อง**ไม่น้อยกว่า**เดือนก่อน | "มิเตอร์ไฟน้อยกว่าเดือนก่อน (X)" |
| มิเตอร์น้ำ (`meter_water`) ต้องเป็นจำนวนเต็ม | "มิเตอร์น้ำต้องเป็นจำนวนเต็มเท่านั้น" |
| ถ้ามี `prev_water > 0` → มิเตอร์น้ำต้องไม่น้อยกว่าเดือนก่อน | "มิเตอร์น้ำน้อยกว่าเดือนก่อน (X)" |
| วันอ่านไฟต้องเป็นรูปแบบ `YYYY-MM-DD` เท่านั้น (regex ตรงเป๊ะ) หรือว่างได้ | "รูปแบบวันอ่านไฟไม่ถูกต้อง (ต้องเป็น YYYY-MM-DD)" |
| ถ้ามีวันอ่านไฟ ต้องอยู่ในช่วง "วันที่ 1 ของเดือนก่อนหน้า" ถึง "วันสุดท้ายของเดือนที่เลือกบันทึก" | "วันอ่านไฟ (X) ไม่อยู่ในช่วง A – B" |
| วันอ่านน้ำ กฎเดียวกันทั้งหมด | "รูปแบบวันอ่านน้ำไม่ถูกต้อง..." / "วันอ่านน้ำ (X) ไม่อยู่ในช่วง..." |
| ถ้างวดนั้นมีใบแจ้งหนี้แล้ว (`has_invoice=true`) | "มีใบแจ้งหนี้แล้ว ไม่สามารถนำเข้าข้อมูลได้" |

แถวที่มี error อย่างน้อย 1 ข้อ → ทั้งแถวถูก**ข้าม** ไม่ถูกส่งไป backend เลย (ไม่ได้ถูกส่งแบบ partial-row) แถวที่ไม่ error จะถูกเติม `_remark` (สร้างอัตโนมัติ), `_common_area`, `_garbage` (ดึงจาก `ali_product.current_central`/`current_waste`) แล้วเก็บเข้า `importSaveableRows[]`

**หมายเหตุ:** ตัวเลขมิเตอร์ที่กรอกเป็น **`0`** ถือว่าผ่าน validate จำนวนเต็มปกติ (ไม่มีกฎห้ามเป็น 0 หรือบังคับต้อง > 0) — เว้นว่างในไฟล์ Excel (`null`/`''`) → `parseImportInt()` คืนค่า `0` (ไม่ใช่ error, ไม่ใช่ `null`)

### 5.4 ฟอร์มนำเข้า Excel — Validation ฝั่ง Backend (`bulkSaveFromExcel`)

Backend **ตรวจซ้ำน้อยกว่าฝั่ง client มาก** — ต่อแถวตรวจแค่:
1. `pcode` ต้องไม่ว่าง (ถ้าว่าง → exception `'pcode ว่าง'`)
2. เช็คมีใบแจ้งหนี้ของงวดนั้นหรือยัง (`me_invoice`) → ถ้ามี exception `'มีใบแจ้งหนี้แล้ว ไม่สามารถแก้ไขข้อมูลมิเตอร์ได้'`

**ไม่มีการตรวจซ้ำเรื่อง**: รูปแบบวันที่, ค่ามิเตอร์ต้องไม่น้อยกว่าเดือนก่อน, pcode มีอยู่จริงหรือไม่ (backend เชื่อว่า client กรองมาแล้ว) — ถ้า client ถูก bypass (เช่นเรียก API ตรง) จะสามารถส่งข้อมูลผิดปกติเข้าระบบได้

---

## 6. เงื่อนไข Business Logic แบบละเอียด

### 6.1 บันทึกเดี่ยว (`saveMeter`)

ลำดับการทำงานเมื่อกด "บันทึก" ในหน้า modal:

1. อ่านค่าจาก `$_POST` ทั้งหมด (ตามหัวข้อ 5.1) — ค่าที่เป็น `''` ถูกแปลงเป็น `null` สำหรับ `electricity/water/garbage/common_area/dateMeterElectricity/dateMeterWater/old_meter_final/new_meter_start`
2. Validate ขั้นต่ำ (`pcode`, `month != 0`, `year != 0`) — ถ้าไม่ผ่านคืน error ทันที ไม่มีการเขียนอะไรลง DB
3. **อัปโหลดรูป** (ถ้ามีไฟล์แนบ) ผ่าน `saveMeterImage()` — ทำ **ก่อน** เขียน DB ทุกครั้ง (ดูหัวข้อ 6.5)
4. เตรียม path รูปที่จะบันทึกจริง: `$electricityImagePath ?: $currentElectricityImage` — ใช้รูปใหม่ที่เพิ่งอัปโหลดถ้ามี ไม่งั้น fallback ไปที่ path เดิมซึ่งส่งมาจาก hidden field (`current_electricity_image`) กรณีผู้ใช้ไม่ได้เปลี่ยนรูป (ถ้าผู้ใช้กดปุ่ม "ลบรูป" ฝั่ง client จะเคลียร์ hidden field เป็น `''` → บันทึกทับเป็นไม่มีรูป)
5. เรียก `saveOrUpdateMeterWithImage()` แยก **2 ครั้งอิสระกัน** สำหรับ `'ค่าไฟ'` และ `'ค่าน้ำ'` — **แต่ละประเภทเป็น transaction เดี่ยว ๆ ไม่มี wrapping transaction ร่วมกัน** ถ้าฝั่งไฟสำเร็จแต่ฝั่งน้ำ error จะได้ผลลัพธ์ที่ไฟถูกบันทึกแต่น้ำไม่ถูกบันทึก (ข้อมูลไม่ atomic)
6. เรียก `saveOrUpdateOtherCostWithRemark()` แยก 2 ครั้งสำหรับ `'ค่าขยะ'` และ `'ค่าส่วนกลาง'` — เช่นเดียวกัน ไม่ atomic ร่วมกับขั้นตอนก่อนหน้า
7. เก็บผลลัพธ์แต่ละขั้นตอนไว้ใน `$allSuccess`/`$errors[]` — ถ้ามีขั้นตอนใดล้มเหลว จะยัง**ทำขั้นตอนถัดไปต่อ**ไม่หยุด (ไม่ rollback ที่ทำสำเร็จไปแล้วก่อนหน้า) สุดท้ายส่ง response `success:false` พร้อมรวม error message แต่ข้อมูลบางส่วนอาจถูกเขียนลง DB ไปแล้ว
8. `updateLatestMeterValues($pcode, $electricity, $water)` — อัปเดตแคช `ali_product.meter_1_latest`/`meter_0_latest` **เฉพาะเมื่อค่านั้นไม่ใช่ `null` และมากกว่า 0** (ค่า 0 หรือ null จะไม่ไปทับค่าที่เคยแคชไว้ — ป้องกันแคชกลายเป็น 0 โดยไม่ตั้งใจ)
9. ถ้า `garbage !== null || common_area !== null` → `updateCurrentCosts()` เขียนทับ `ali_product.current_waste`/`current_central` (ใช้ 0 แทนค่าที่เป็น null) — ค่านี้จะกลายเป็น**ค่า default สำหรับห้องนั้นในงวดถัดไป**
10. คืน JSON `success/message` (มี debug info: `error_detail`, `error_code`, `debug_info.{pcode,month,year}` เมื่อเกิด `PDOException`)

**สรุปพฤติกรรม "แก้ไข" (edit) — จริง ๆ แล้วใช้ endpoint เดียวกับบันทึกใหม่ทั้งหมด (`saveMeter`)** เพราะ `saveOrUpdateMeterWithImage`/`saveOrUpdateOtherCostWithRemark` ทำ upsert (เช็คมีอยู่แล้วหรือไม่ผ่าน `meterRecordExists`/`SELECT COUNT`) ไม่มี endpoint แยกสำหรับ "insert" กับ "update"

#### 6.1.1 เงื่อนไขพิเศษ: มีใบแจ้งหนี้แล้ว

- Frontend เช็ค `checkInvoiceExists` **ก่อนเปิด modal ทุกครั้ง** และปรับ UI เป็น read-only (ซ่อนปุ่มบันทึก) ถ้ามีใบแจ้งหนี้แล้ว
- **Backend ของ `saveMeter()` ไม่ได้เช็คซ้ำว่ามีใบแจ้งหนี้หรือไม่** — ถ้ามีการเรียก endpoint นี้ตรง (bypass UI) จะยังสามารถบันทึก/แก้ไขข้อมูลมิเตอร์ของงวดที่มีใบแจ้งหนี้แล้วได้ (ตรงข้ามกับ `bulkSaveFromExcel` และ `delete` ที่มีการเช็คซ้ำที่ backend) — **นี่คือช่องโหว่ด้าน consistency ที่ต้องแก้ไขในระบบใหม่**

### 6.2 แก้ไข (Edit) — flow การเปิด modal

1. คลิกปุ่มแก้ไข → ยิง `checkInvoiceExists` → ได้ `isReadOnly`
2. `openEditMeterModal(meter, isReadOnly)` เติมค่าพื้นฐานจาก object `meter` (มาจาก cache `allMeters` ที่โหลดครั้งล่าสุดจาก `getByPeriod`, ไม่ได้ fetch ใหม่)
3. รีเซ็ต checkbox รีเซ็ต/เปลี่ยนมิเตอร์และ section ที่เกี่ยวข้องเป็นค่าว่างก่อนเสมอ
4. ตั้ง readonly/disabled ทุกฟิลด์ตาม `isReadOnly` (รายละเอียดหัวข้อ 4.4)
5. ถ้าไม่ใช่ read-only และค่าขยะ+ค่าส่วนกลางที่โหลดมาเป็น 0 ทั้งคู่ → auto-fetch จาก `getCurrentCosts` มาเติม (เฉพาะค่าที่ > 0)
6. `loadMeterImages()` → เรียก `getMeterImages` แสดงรูปที่เคยอัปโหลด
7. `loadMeterResetChangedData()` → เรียก `getMeterResetChanged` เติมค่า checkbox/ช่องเลขเก่า-ใหม่ตามข้อมูลจริงใน DB (**overwrite ค่าที่ reset ไปแล้วในขั้นตอน 3** ถ้ามีข้อมูลจริง)
8. ถ้า `meter.status === 'saved'` → `loadLastUpdatedInfo()` แสดงผู้บันทึกล่าสุด, ถ้าไม่ใช่ → เคลียร์พื้นที่นั้นว่าง
9. แสดง modal

### 6.3 ลบ (`delete`)

1. Frontend: คลิกปุ่มลบ → ยิง `checkInvoiceExists` ก่อนเสมอ
   - ถ้ามีใบแจ้งหนี้ → SweetAlert "ไม่สามารถลบได้ ข้อมูลมิเตอร์นี้มีใบแจ้งหนี้แล้ว กรุณาลบใบแจ้งหนี้ก่อน" — **หยุดทันที ไม่ยิง POST /meters/delete เลย**
   - ถ้าไม่มี → SweetAlert ยืนยันการลบ (แสดง pcode/เดือน/ปี) → ถ้ายืนยัน ส่ง `POST /meters/delete` (FormData: `pcode`, `month`, `year`)
2. Backend `delete()`:
   - Validate: `pcode` ไม่ว่าง, `month` 1–12, `year` 2000–2100
   - **ตรวจซ้ำอีกครั้ง**ว่ามีใบแจ้งหนี้หรือไม่ (`me_invoice`) → ถ้ามี throw `'ไม่สามารถลบข้อมูลมิเตอร์ได้ เนื่องจากมีใบแจ้งหนี้แล้ว กรุณาลบใบแจ้งหนี้ก่อน'` (double-check ที่ backend — endpoint นี้ทำถูกต้องกว่า `saveMeter`)
   - ลบ `me_meter_ohter` (ค่าขยะ+ส่วนกลาง) ของ `pcode/month/year` นั้นก่อน — **เป็นการลบแบบ best-effort**: ถ้า error จะแค่ log ไว้ ไม่ throw ต่อ (ทำให้ยังลบ `me_meter` ต่อได้แม้ลบ `me_meter_ohter` ไม่สำเร็จ — อาจเหลือข้อมูลขยะ/ส่วนกลางค้างอยู่โดยไม่มีมิเตอร์คู่กัน)
   - เรียก `$this->meterModel->deleteMeter($pcode, $month, $year)`:
     - เช็คก่อนว่ามีแถวใน `me_meter` สำหรับ key นี้หรือไม่ (`COUNT`) — ถ้าไม่มีคืน `false`
     - `DELETE FROM me_meter WHERE pcode=? AND month=? AND year=?` — **ลบทั้ง `'ค่าไฟ'` และ `'ค่าน้ำ'` พร้อมกันในคำสั่งเดียว** (ไม่มี `meter_type` ใน WHERE) — ไม่สามารถลบแยกเฉพาะไฟหรือเฉพาะน้ำได้ผ่าน endpoint นี้ ลบคือลบทั้งคู่
     - คืน `true` เฉพาะเมื่อ `rowCount() > 0`
   - ตอบกลับ success/error

**ผลลัพธ์เมื่อลบสำเร็จฝั่ง client:** `window.location.reload()` (โหลดหน้าใหม่ทั้งหมด ไม่ใช่แค่ re-fetch ตาราง)

### 6.4 บันทึกจาก Excel แบบกลุ่ม (Bulk Import)

**ขั้นตอนเต็ม (client-driven, ไม่มี server-side upload ไฟล์ Excel เลย — อ่านไฟล์ทั้งหมดในเบราว์เซอร์):**

1. เปิด modal → sync เดือน/ปีจากตัวกรองหลักของหน้า
2. เลือกไฟล์ (drag-drop หรือคลิก) → ตรวจนามสกุลไฟล์ (`xlsx`/`xls`) ที่ client
3. `readImportExcelFile()`: อ่านด้วย **SheetJS (XLSX.js, โหลดจาก CDN `cdnjs.cloudflare.com`)**, `sheet_to_json({header:1, raw:false, dateNF:'yyyy-mm-dd'})`, ข้ามแถว header (index 0), แถวที่คอลัมน์ A ว่างจะถูกข้าม (ไม่ถือเป็น error, เงียบ ๆ ไม่นับ)
4. แปลงค่าแต่ละ cell: `parseImportDate()` (รับเฉพาะ `YYYY-MM-DD` เป๊ะ, ว่าง=`''` ผ่านได้, อื่น ๆ = `'__INVALID__'`), `parseImportInt()` (รองรับ comma-strip, ว่าง=`0`, ไม่ใช่ตัวเลขเต็ม=`'__INVALID__'`)
5. ถ้าไม่มีแถวข้อมูลเลยหลังกรอง → SweetAlert "ไม่พบข้อมูล กรุณาตรวจสอบรูปแบบ (เริ่มข้อมูลแถวที่ 2)"
6. `fetchImportPreviewData()`: เก็บ pcode ไม่ซ้ำทั้งหมดจากไฟล์ → ยิง `GET excel-preview-data` ครั้งเดียว (batch) ได้ข้อมูลอ้างอิงของทุก pcode
7. `buildImportPreviewTable()`: validate ทุกแถว (กฎเต็มในหัวข้อ 5.3), เรนเดอร์ตาราง preview พร้อม highlight เขียว (ผ่าน)/แดง (error), สร้างหมายเหตุอัตโนมัติต่อแถวที่ผ่าน (`generateImportRemark()` — สูตรเดียวกับ `generateRemarkText()` ฝั่ง modal เดี่ยว), ดึงรูปเดิม (ถ้ามี) มาตั้งต้นใน `importRowImages`
8. ผู้ใช้สามารถแนบรูปมิเตอร์ไฟ/น้ำรายแถวได้ในตาราง preview — เมื่อเลือกไฟล์จะเรียก `importSetImage()` → ยิง `uploadTempImage` **ทันที** (อัปโหลดจริงลง disk ก่อนกด "บันทึก" หลัก — ถ้าผู้ใช้ปิด modal โดยไม่กดบันทึกจะเหลือไฟล์ orphan อยู่ในโฟลเดอร์อัปโหลด)
9. กด "บันทึก N รายการ" (`saveAllImport()`): ยืนยันด้วย SweetAlert (แสดงจำนวนแถวและเดือน/ปี) → แสดง progress bar จำลอง (fake progress, ไม่ใช่ progress จริงจาก server — เพิ่มค่า mock ทุก 120ms จนถึง ~88% แล้วรอ response) → ส่ง `POST bulk-save-from-excel` เป็น **JSON body เดียวทั้งหมด** (ไม่ใช่หลาย request)
10. Backend `bulkSaveFromExcel()` loop ทีละแถว, **catch exception ต่อแถว** (แถวหนึ่งพังไม่กระทบแถวอื่น) นับ `successCount`/`errorCount`, เก็บ error message ราย pcode
11. ทุกแถวที่ผ่าน backend loop (ไม่ throw) จะถูก:
    - บันทึกไฟ (`meter_reset=0, meter_changed=0, old_meter_final=null, new_meter_start=null` **เสมอ** — **การ import Excel ไม่รองรับ flow มิเตอร์รีเซ็ต/เปลี่ยนมิเตอร์เลย**)
    - บันทึกน้ำ (เงื่อนไขเดียวกัน)
    - บันทึกค่าขยะ/ส่วนกลาง เฉพาะถ้าไม่ใช่ `null` (มาจาก `_garbage`/`_common_area` ที่ preview ดึงจาก `ali_product.current_waste/current_central` — **ผู้ใช้ import Excel ไม่สามารถกำหนดค่าขยะ/ส่วนกลางเองในไฟล์ได้ ระบบจะใช้ค่า default ล่าสุดของห้องนั้นเสมอ**)
    - อัปเดตแคช `meter_1_latest`/`meter_0_latest`
    - อัปเดต `current_waste`/`current_central`
12. คืนผลลัพธ์ `{success, successCount, errorCount, errors[]}` — client แสดงสรุปด้วย SweetAlert แล้ว `loadMeters()` รีเฟรชตาราง (reload แบบ AJAX ไม่ reload หน้า)

**ความแตกต่างสำคัญระหว่าง "บันทึกเดี่ยว" กับ "บันทึกกลุ่มจาก Excel":**

| ประเด็น | บันทึกเดี่ยว (`saveMeter`) | บันทึกกลุ่ม (`bulkSaveFromExcel`) |
|---|---|---|
| เช็คใบแจ้งหนี้ที่ backend | **ไม่เช็ค** | เช็คทุกแถว |
| รองรับมิเตอร์รีเซ็ต/เปลี่ยนมิเตอร์ | รองรับเต็มรูปแบบ | **ไม่รองรับ (บังคับ 0/null เสมอ)** |
| ค่าขยะ/ส่วนกลาง | ผู้ใช้กรอกเองในฟอร์ม | ดึงอัตโนมัติจาก `ali_product` เท่านั้น ผู้ใช้กำหนดเองไม่ได้ผ่าน Excel |
| การ validate เลขมิเตอร์ต้อง ≥ เดือนก่อน | ไม่มีการ validate ที่ backend เลย | validate เข้มที่ **client** เท่านั้น (backend ไม่เช็คซ้ำ) |
| Atomic ต่อรายการ | ไม่ atomic ระหว่าง 4 ส่วน (ไฟ/น้ำ/ขยะ/ส่วนกลาง) ของห้องเดียวกัน | เช่นเดียวกัน แต่ error ของแต่ละห้องไม่กระทบห้องอื่น |
| รูปภาพ | อัปโหลดพร้อมกับฟอร์ม (multipart) | อัปโหลดล่วงหน้าทีละไฟล์ผ่าน `uploadTempImage` แล้วส่ง path มาพร้อม JSON |

### 6.5 การอัปโหลดรูปภาพมิเตอร์

**Helper กลาง:** `saveMeterImage($fieldName, $pcode, $month, $year, $type)` (private method ใน controller) ใช้ร่วมกันทั้ง `saveMeter()` (field `img_electricity`/`img_water`) และ `uploadTempImage()` (field `img_file`)

**กฎการอัปโหลด:**
- ต้องมีไฟล์และ `$_FILES[...]['error'] === UPLOAD_ERR_OK` มิฉะนั้นคืน `null` (ไม่ error, แค่ข้าม)
- **ตรวจชนิดไฟล์จาก `$_FILES[...]['type']` (MIME type ที่ browser ส่งมาเอง)** — อนุญาตเฉพาะ `image/jpeg`, `image/png`, `image/gif` — **นี่คือการเชื่อ client-supplied header ล้วน ๆ ไม่มีการตรวจ magic byte จริงของไฟล์ (`getimagesize()`/`finfo`) จึงปลอมแปลงได้ง่าย** — ต้องแก้ไขจุดนี้ในระบบใหม่
- ขนาดไฟล์ต้องไม่เกิน **5 MB** (`5 * 1024 * 1024` bytes) เช็คจาก `$_FILES[...]['size']`
- โฟลเดอร์ปลายทาง: `public/uploads/meters/` (สร้างอัตโนมัติด้วย `mkdir(...,0755,true)` ถ้ายังไม่มี)
- ชื่อไฟล์ที่บันทึก: `{safePcode}_{month}_{year}_{type}_{timestamp}.{ext}` โดย `safePcode` = `pcode` ที่แทนอักขระอื่นนอกเหนือ `[a-zA-Z0-9_-]` ด้วย `_` (regex `preg_replace('/[^a-zA-Z0-9_-]/', '_', $pcode)`), `type` = `'electricity'` หรือ `'water'`, `timestamp` = `time()` (วินาที — โอกาสชนกันต่ำมากแต่ทางทฤษฎีชนได้ถ้ายิงพร้อมกันในวินาทีเดียวกันสำหรับ pcode/month/year/type เดียวกัน), นามสกุลไฟล์ตามที่ผู้ใช้อัปโหลดมา (`pathinfo($file['name'], PATHINFO_EXTENSION)` — **ไม่ normalize เป็นตัวพิมพ์เล็ก และไม่ validate ว่านามสกุลต้องตรงกับ MIME type ที่เช็คไปก่อนหน้า**)
- บันทึกด้วย `move_uploaded_file()`; คืน path แบบ relative `"/uploads/meters/{filename}"` (ไม่รวม `basePath` ของระบบ — ฝั่ง client เป็นคนต่อ `basePath` เข้าไปเองตอนแสดงผล เช่น `${basePath}${result.data.electricityImage}`)
- ถ้าอัปโหลดล้มเหลว (ประเภทผิด/ขนาดเกิน/`move_uploaded_file` fail) → คืน `null` เงียบ ๆ ที่ `saveMeter()` (ไม่ error กลับไปที่ผู้ใช้ แค่ไม่บันทึกรูปใหม่ และจะ fallback ไปใช้ `current_electricity_image`/`current_water_image` ที่ส่งมาแทน) — ส่วน `uploadTempImage()` จะแปลงเป็น error message ให้ผู้ใช้เห็นชัดเจนกว่า ("บันทึกรูปไม่สำเร็จ กรุณาตรวจสอบประเภทไฟล์ (JPG/PNG/GIF) และขนาด (ไม่เกิน 5MB)")

**จำนวนรูปต่อการอ่านมิเตอร์ 1 ครั้ง:** สูงสุด **1 รูปต่อประเภท (ไฟ 1 รูป, น้ำ 1 รูป)** ต่องวด — ไม่รองรับ multiple images ต่อประเภท ทุกครั้งที่อัปโหลดใหม่จะ**ทับ path เดิม** ใน `me_meter.img` (ไฟล์เก่าที่เคยอัปโหลดไว้ในดิสก์**ไม่ถูกลบทิ้ง** เป็น orphan file สะสมในโฟลเดอร์ `public/uploads/meters/`)

**การลบรูป (client-side):** ปุ่ม "ลบรูป" (`clearElectricityImage()`/`clearWaterImage()`) แค่เคลียร์ preview + hidden field (`currentElectricityImage`/`currentWaterImage`) ในฟอร์ม ณ ขณะนั้น **ไม่ได้ลบไฟล์จริงออกจากดิสก์ ไม่ได้ยิง API ใด ๆ ทันที** — การลบจริงในฐานข้อมูลเกิดขึ้นเมื่อกด "บันทึก" เท่านั้น (เพราะ `img` field จะถูกส่งเป็นค่าว่างไปเขียนทับ)

**Import Excel:** รูปถูกอัปโหลดผ่าน `uploadTempImage` (ใช้ helper เดียวกัน) ทันทีที่เลือกไฟล์ในตาราง preview — เก็บ path ไว้ใน `importRowImages[idx][type]` แล้วส่งไปพร้อม payload ตอน `saveAllImport()` ถ้าไม่ได้เลือกไฟล์ใหม่รายแถว จะใช้รูปเดิม (`db.img_electricity`/`db.img_water` จาก preview data) เป็นค่า default

---

## 7. การคำนวณ (สูตรทั้งหมด)

### 7.1 หน่วยใช้ไฟฟ้า/น้ำแบบปกติ (ไม่มีรีเซ็ต ไม่มีเปลี่ยนมิเตอร์)

```
units = current_reading - previous_reading
if units < 0 → units = 0   (ป้องกันหน่วยติดลบ, log warning)
cost = units * price_per_unit
```

`price_per_unit` = `ali_product.meter_1_ppu` (ไฟ) หรือ `meter_0_ppu` (น้ำ) — ดึงมาพร้อมสินค้าตั้งแต่ query หลักของ `getAllMeters()`

`previous_reading` มาจาก `me_meter.reading_value` ของ**เดือนก่อนหน้างวดที่กำลังแสดง** (`month-1`, wrap ปีถ้า `month-1 < 1` → เป็นเดือน 12 ปีก่อนหน้า) ถ้าไม่มีข้อมูลเดือนก่อน (ห้องใหม่/ยังไม่เคยจด) → `previous = 0`

### 7.2 กรณี "มิเตอร์รีเซ็ต" (`meter_reset = 1`) — ใช้ในหน้าจอหลัก (`Meter::calculateElectricityCostBatch`/`calculateWaterCostBatch`)

```
maxMeterValue = 9999                              // ค่า default (มิเตอร์ 4 หลัก)
if previous >= 10000 → maxMeterValue = 99999      // มิเตอร์ 5 หลัก
else if previous >= 1000 → maxMeterValue = 9999   // มิเตอร์ 4 หลัก (ค่าเดียวกับ default)
else if previous >= 100  → maxMeterValue = 999    // มิเตอร์ 3 หลัก
// หมายเหตุ: ถ้า previous < 100 จะไม่มี branch ใดถูกเข้าเลย → maxMeterValue คงค่า default = 9999

percentFull = (previous / maxMeterValue) * 100

if percentFull > 90:
    // ถือว่าเป็นการ "รีเซ็ตจริง" (มิเตอร์หมุนครบรอบ)
    units = (maxMeterValue - previous) + current + 1
else:
    // ถือว่าเป็น "มิเตอร์ใหม่" ไม่ใช่การรีเซ็ตจริง (previous ยังไม่ใกล้เต็ม)
    units = current   // ใช้ค่า current ตรง ๆ เป็นหน่วยที่ใช้ (ไม่ลบ previous ออก)

if units < 0 → units = 0
cost = units * price_per_unit
```

**หมายเหตุสำคัญ (edge case ของสูตร):**
- เกณฑ์ `percentFull > 90` เป็นการ**เดา (heuristic)** ว่ามิเตอร์รีเซ็ตเพราะหมุนครบรอบจริง (เลขเดือนก่อนใกล้ค่าสูงสุดของมิเตอร์) เทียบกับผู้ใช้ติ๊ก "รีเซ็ต" ผิดทั้งที่จริงเป็นมิเตอร์ใหม่/เพิ่งเริ่มนับ (เลขเดือนก่อนยังต่ำอยู่) — ถ้าเดือนก่อนไม่ใกล้เต็ม ระบบจะเปลี่ยนวิธีคิดเป็น "units = current" แทนโดยอัตโนมัติ (ไม่ error, ไม่แจ้งเตือนผู้ใช้)
- สูตร "รีเซ็ตจริง" บวก `+1` เข้าไปเสมอ (คิดว่ามิเตอร์นับจาก 0 ไปจนครบ maxMeterValue แล้ววนกลับมาที่ 0 อีกครั้งแล้วเดินต่อถึง current — ตรรกะ `(maxMeterValue - previous) + current + 1`)
- ค่า `maxMeterValue` ตายตัวเป็น `9999`/`99999`/`999` เท่านั้น (ไม่มี option มิเตอร์ 6 หลักขึ้นไป หรือ 1-2 หลัก)
- **ไฟล์ export Excel รายงานตามช่วง (`views/pages/reports/meterExcel.php`, ฟังก์ชัน `calculateMeterUnits`) ใช้เกณฑ์ `percentFull >= 95` (ไม่ใช่ `> 90`) และไม่มี branch "NEW METER" fallback (ถ้าไม่เข้าเงื่อนไข `>= 95` จะตกไปที่ logic ปกติ/เปลี่ยนมิเตอร์แทน)** — **นี่คือความไม่สอดคล้องกัน (inconsistency) ระหว่างสูตรคำนวณของหน้าจอหลักกับของรายงาน Excel ต้องตัดสินใจในระบบใหม่ว่าจะยึดเกณฑ์ไหนเป็นมาตรฐานเดียว**
- ไฟล์ export Excel อีกไฟล์หนึ่ง (`reports/meterExcel.php`, ส่วน Sheet 2 รายละเอียดรายเดือน) ใช้สูตรง่ายกว่ามาก: ถ้า `meter_reset==1` → `prevReading = 0; units = currentReading` ตรง ๆ (**ไม่มี logic ตรวจ percentFull เลยในจุดนี้ — เป็นสูตรที่ 3 ที่ต่างจากทั้งสองข้างต้น**)

### 7.3 กรณี "เปลี่ยนมิเตอร์" (`meter_changed = 1` และมีทั้ง `old_meter_final`, `new_meter_start`)

```
units_from_old_meter = old_meter_final - previous_reading
units_from_new_meter = current_reading - new_meter_start
units = units_from_old_meter + units_from_new_meter

if units < 0 → units = 0
cost = units * price_per_unit
```

ตรรกะ: มิเตอร์เก่าเดินจาก `previous_reading` (เลขเดือนก่อน) ไปจนถึง `old_meter_final` (เลขสุดท้ายก่อนถอดมิเตอร์เก่าออก) แล้วมิเตอร์ใหม่เริ่มนับจาก `new_meter_start` ไปจนถึง `current_reading` (เลขที่จดได้งวดนี้) — รวมหน่วยทั้งสองช่วงเข้าด้วยกัน

**เงื่อนไขการเข้าสูตรนี้ในโมเดล:** ต้องผ่านการเช็ค `meterReset != 1` ก่อน (เพราะ `if ($meterReset==1) {...} else if ($meterChanged==1 && ...) {...} else {...}`) — **นั่นคือถ้า `meter_reset=1` ถูกตั้งไว้พร้อมกับ `meter_changed=1` (กรณีผิดปกติที่ backend ไม่ป้องกัน) ระบบจะใช้สูตร "รีเซ็ต" เป็นหลักเสมอ (reset ชนะ changed ในลำดับความสำคัญ)** แม้ UI ปกติจะบังคับให้ติ๊กได้แค่อันเดียว (ผ่าน JS uncheck คู่แข่ง) — ถ้า flag ทั้งคู่หลุดเป็น 1 พร้อมกันในฐานข้อมูล (เช่นจาก bug อื่นหรือแก้ไขข้อมูลตรง) ผลการคำนวณจะพึ่งพา priority นี้

### 7.4 ยอดรวมของห้องหนึ่งในหนึ่งงวด

```
total = electricity_cost + water_cost + garbage_price + common_area_price
```

`garbage_price`/`common_area_price` **ไม่มีการคำนวณหน่วยใด ๆ** — เป็นค่าเงินที่ผู้ใช้กรอกตรง ๆ (หรือดึงจาก `ali_product.current_waste`/`current_central` เป็นค่า default) ไม่ใช่ราคา × หน่วยเหมือนไฟ/น้ำ

### 7.5 สถิติรวมทั้งหน้า (คำนวณฝั่ง client จากทุกแถวที่กรองอยู่)

```
total_meters   = count(rows)
saved_count    = count(rows where status == 'saved'|true|1|'1')
unsaved_count  = count(rows) - saved_count
total_electricity = Σ electricity  (ต่อแถว, parseFloat, NaN→0)
total_water        = Σ water
total_garbage      = Σ garbage
total_common_area  = Σ common_area
grand_total = total_electricity + total_water + total_garbage + total_common_area
```

**หมายเหตุ:** ถ้างวดที่เลือกอยู่ก่อน 9/2025 backend จะ force ค่า `electricity/water/garbage/common_area/total` เป็น 0 มาตั้งแต่ response ของ `getByPeriod` แล้ว (ตามหัวข้อ 2.2 ข้อ 5) ทำให้สถิติหน้านี้เป็น 0 ไปด้วยโดยอัตโนมัติ (ไม่ได้คำนวณ 0 เองที่ client)

### 7.6 มินิมัมชาร์จ (Minimum Charge)

**ไม่มีสูตร minimum charge ปรากฏในโค้ดของโมดูลนี้เลย** — ไม่มีการบังคับค่าไฟ/น้ำขั้นต่ำต่อเดือน ระบบคิดตามหน่วยจริงที่คำนวณได้เท่านั้น (ถ้า `units=0` → `cost=0`)

---

## 8. เงื่อนไขการแสดงผลและสถานะ

### 8.1 สถานะ "บันทึกแล้ว (saved)" vs "ยังไม่บันทึก (unsaved)"

กำหนดที่ `Meter::hasMeterDataBatch($pcode, $meterData, $otherCosts)`:

```
status = 'saved'   ถ้ามีแถวอย่างน้อย 1 แถวใน me_meter (ไฟหรือน้ำ) ของงวดนี้
                    หรือมีอย่างน้อย 1 แถวใน me_meter_ohter (ขยะหรือส่วนกลาง) ของงวดนี้
status = 'unsaved' ถ้าไม่มีข้อมูลเลยทั้งสองตารางสำหรับ pcode/month/year นี้
```

**หมายเหตุ:** แค่มีแถวอยู่ (แม้ `reading_value`/`price` จะเป็น `NULL` ทั้งหมดก็ตาม) ก็ถือว่า `saved` แล้ว — ไม่ได้เช็คว่าค่ามีความหมาย (>0) หรือไม่

### 8.2 การแสดงผลปุ่มลบ

- แถวสถานะ `saved` → ปุ่มลบ enable
- แถวสถานะ `unsaved` → ปุ่มลบแสดงแต่ `disabled` (มองเห็นแต่กดไม่ได้) — ป้องกันลบข้อมูลที่ไม่มีอยู่จริง

### 8.3 การแสดงผลปุ่มแก้ไข/ไอคอน

- ปกติ: ไอคอนดินสอ, title "แก้ไข"
- มีใบแจ้งหนี้แล้ว (เช็คจาก `meter.hasInvoice` ที่แนบมากับข้อมูลตารางตอนโหลด และเช็คซ้ำอีกครั้งตอนคลิกผ่าน `checkInvoiceExists`): ไอคอนตา, title "ดูข้อมูล", เปิด modal โหมด read-only

### 8.4 การแสดงผลก่อนจุดเริ่มต้นระบบ (< 09/2025)

- ตารางยังคงแสดงแถวข้อมูลปกติ (pcode, สถานะ, เลขมิเตอร์)
- แต่ **ยอดเงินทุกคอลัมน์ (ค่าไฟ/น้ำ/ขยะ/ส่วนกลาง/รวม) และสถิติสรุปด้านบนจะเป็น 0 เสมอ** (บังคับที่ backend ก่อนส่ง response)
- Dropdown ปีในตัวกรองเริ่มต้นที่ปี **2025** เป็นปีแรกสุดที่เลือกได้ (ไม่มีปีก่อนหน้าให้เลือกใน UI เลย ถึงแม้ backend จะรองรับ query ปีใด ๆ ก็ตามถ้าถูกเรียกตรง)

### 8.5 แสดง/ซ่อน Section เปลี่ยนมิเตอร์

`#electricityMeterChangeSection`/`#waterMeterChangeSection` เริ่มต้น `display:none` แสดงเป็น `display:flex` เฉพาะเมื่อ:
- ผู้ใช้ติ๊ก checkbox "เปลี่ยนมิเตอร์" เอง, หรือ
- ข้อมูลที่โหลดจาก `getMeterResetChanged` พบว่า `meter_changed == 1` ในฐานข้อมูลของงวดนั้น (auto-expand ให้เห็นค่าที่เคยบันทึกไว้)

---

## 9. กรณีมิเตอร์เปลี่ยน/รีเซ็ต (สรุปเจาะจง)

โมดูลนี้แยกแยะ 2 สถานการณ์ที่ทำให้ "เลขมิเตอร์ปัจจุบันน้อยกว่าเลขมิเตอร์เดือนก่อน" ไม่ได้แปลว่าใช้ไฟ/น้ำติดลบ:

### 9.1 มิเตอร์รีเซ็ต (`meter_reset`)

ใช้เมื่อ **มิเตอร์ตัวเดิม** หมุนครบรอบ (เช่นมิเตอร์เลขวิ่งถึงค่าสูงสุดของมิเตอร์แล้ววนกลับมาเริ่มที่ 0 ใหม่ — กรณีทั่วไปของมิเตอร์เชิงกลรุ่นเก่า) — ไม่ได้เปลี่ยนตัวมิเตอร์จริง

- Input ที่ผู้ใช้ต้องกรอกเพิ่ม: **ไม่มี** (แค่ติ๊ก checkbox, backend คำนวณ `maxMeterValue` เองจากขนาดของเลขเดือนก่อน)
- ระบบจะเดา (heuristic) ว่าเป็นรีเซ็ตจริงหรือไม่จาก `percentFull > 90%` ของเดือนก่อนเทียบกับ maxMeterValue ที่เดาจากจำนวนหลัก — ถ้าไม่เข้าเงื่อนไข ระบบจะเปลี่ยนไปคำนวณแบบ "มิเตอร์ใหม่" (`units = current`) แทนโดยไม่แจ้งผู้ใช้ (ดูหัวข้อ 7.2)
- ฟิลด์ที่บันทึกลง DB: `meter_reset = 1`, `meter_changed` ถูกบังคับเป็น `0` (ฝั่ง JS uncheck คู่แข่งเอง), `old_meter_final`/`new_meter_start` ถูกเคลียร์เป็นค่าว่าง

### 9.2 เปลี่ยนมิเตอร์ (`meter_changed`)

ใช้เมื่อ **เปลี่ยนตัวมิเตอร์ใหม่จริง ๆ** (มิเตอร์เก่าเสีย/ถูกถอดเปลี่ยน) ในช่วงกลางงวด — ต้องมีข้อมูลรอยต่อ 2 ค่า:
- **มิเตอร์เก่าสุดท้าย** (`old_meter_final`) — เลขที่มิเตอร์เก่าอ่านได้ครั้งสุดท้ายก่อนถอด
- **มิเตอร์ใหม่เริ่มต้น** (`new_meter_start`) — เลขเริ่มต้นของมิเตอร์ตัวใหม่ (ปกติมักเป็น 0 แต่ระบบไม่บังคับต้องเป็น 0)

**Validation:** ฝั่ง client บังคับว่าถ้าติ๊ก "เปลี่ยนมิเตอร์" ต้องกรอกทั้งสองค่านี้ (มิฉะนั้นบล็อกการบันทึกด้วย SweetAlert) — **แต่ backend ไม่ enforce ซ้ำเลย** ถ้ากรอกแค่ค่าใดค่าหนึ่งหรือไม่กรอกเลยแล้วยังส่ง `meter_changed=1` ไปตรง ๆ ผ่าน API จะบันทึกลง DB ได้โดยค่าที่ขาดจะเป็น `null` — และตอนคำนวณหน่วยจะ**ไม่เข้าเงื่อนไข "เปลี่ยนมิเตอร์"** (เพราะโมเดลเช็ค `$oldMeterFinal !== null && $newMeterStart !== null` ด้วย) จึงตกไปใช้สูตรปกติแทน (`units = current - previous`) ซึ่งอาจได้ค่าติดลบแล้วถูกบังคับเป็น 0

- ฟิลด์ที่บันทึกลง DB: `meter_changed = 1`, `meter_reset` ถูกบังคับเป็น `0`, `old_meter_final`, `new_meter_start` (int)

### 9.3 ผลกระทบต่อเดือนถัดไป

`getPreviousMonthMeterDataBatch()` ดึงแค่ `reading_value`/`reading_date` ของเดือนก่อนหน้าเพื่อใช้เป็น "previous" — **ไม่ได้ดึง `meter_reset`/`meter_changed`/`old_meter_final`/`new_meter_start` ของเดือนก่อนมาด้วย** เพราะ flag เหล่านี้เป็นของ**งวดปัจจุบัน**เท่านั้น (บอกว่า "งวดนี้มีการรีเซ็ต/เปลี่ยนมิเตอร์" ไม่ใช่สถานะสะสม) ดังนั้นเดือนถัดจากเดือนที่เปลี่ยนมิเตอร์ จะใช้ `reading_value` ของเดือนที่เปลี่ยนมิเตอร์ (คือเลขมิเตอร์ใหม่ที่จดได้ ณ ตอนนั้น) เป็น `previous_reading` ตามปกติในสูตร normal — ไม่ต้องกังวลเรื่อง `old_meter_final` อีกต่อไปในเดือนถัดไป (มันถูกใช้แค่ตอนคำนวณของเดือนที่เปลี่ยนมิเตอร์เดือนเดียว)

### 9.4 การรองรับใน Export/Import

- **Import Excel (bulk):** ไม่รองรับทั้งรีเซ็ตและเปลี่ยนมิเตอร์เลย — ทุกแถวที่ import จะถูกบันทึกด้วย `meter_reset=0, meter_changed=0` เสมอ ถ้าห้องใดมีการเปลี่ยน/รีเซ็ตมิเตอร์ในงวดที่ต้องการ import จะต้องไปแก้ไขผ่าน modal เดี่ยวหลัง import เสร็จเท่านั้น
- **Export Excel:** ทั้งสองไฟล์ export (`views/pages/meterExcel.php` ที่ไม่ได้ใช้งานจริง กับ `views/pages/reports/meterExcel.php` ที่ใช้งานจริง) แสดงข้อความ `"(รีเซ็ต)"` หรือ `"(เปลี่ยนมิเตอร์)"` ต่อท้ายเลขมิเตอร์ก่อนหน้าในชีต "รายละเอียดมิเตอร์" (Sheet 2) แต่ **ใช้สูตรคำนวณหน่วยที่เขียนซ้ำเป็นอิสระจากกันคนละชุด** ต่างจากสูตรในโมเดลหลัก (ดูรายละเอียดหัวข้อ 7.2) — เป็นจุดเสี่ยงข้อมูลไม่ตรงกันระหว่างหน้าจอกับรายงาน export ต้อง unify เป็นฟังก์ชันเดียวในระบบใหม่

---

## 10. Edge cases / ข้อควรระวัง

รวบรวมจุดเสี่ยง, พฤติกรรมแปลก, และความไม่สอดคล้องที่พบจากการอ่านโค้ดจริง (ไม่ใช่การคาดเดา) เพื่อให้ทีมพัฒนาระบบใหม่ตัดสินใจได้ว่าจะ "คงพฤติกรรมเดิม" หรือ "แก้ไขให้ถูกต้องกว่าเดิม":

1. **Route `/meters/edit/{id}`, `/meters/store`, `/meters/update/{id}` ไม่มีเมธอดรองรับในคอนโทรลเลอร์จริง** — เป็น route ค้าง (dead route) จากรูปแบบ CRUD มาตรฐานที่ระบบนี้เปลี่ยนไปใช้ AJAX endpoint แทนหมดแล้ว (`save-meter`, `bulk-save-from-excel`, `delete`) ถ้าเรียกจะ error เมธอดไม่พบ
2. **เมธอด `search()` ในคอนโทรลเลอร์ และ `searchMeter()`/`countMeters()`/`getOtherCost()`/`hasMeterData()`/`saveOrUpdateMeter()` (แบบไม่มี image) ในโมเดล เป็นโค้ดที่ไม่ถูกเรียกใช้จากเส้นทางใดในระบบปัจจุบันเลย** (dead code) — การ audit ก่อน rewrite ควรตัดออก ไม่ต้อง migrate
3. **`saveMeter()` (บันทึกเดี่ยว) ไม่เช็คใบแจ้งหนี้ที่ backend** ต่างจาก `delete()` และ `bulkSaveFromExcel()` ที่เช็ค — ถ้ามีการเรียก API ตรง (ไม่ผ่าน UI) จะแก้ไขข้อมูลมิเตอร์ของงวดที่ออกใบแจ้งหนี้ไปแล้วได้ ทำให้ตัวเลขในใบแจ้งหนี้กับข้อมูลมิเตอร์ไม่ตรงกัน
4. **การบันทึกไม่ atomic** — 1 การกด "บันทึก" ในโมดัลเดี่ยวจริง ๆ เขียนข้อมูล 4-5 ครั้งแยกกัน (ไฟ, น้ำ, ขยะ, ส่วนกลาง, อัปเดตแคช `ali_product`) โดยไม่มี DB transaction ครอบ ถ้า query กลางทางล้มเหลว (เช่น connection หลุด) จะเหลือข้อมูลที่เขียนไปแล้วบางส่วนค้างอยู่ (partial write)
5. **สูตรคำนวณหน่วยตอนมิเตอร์รีเซ็ตมี 3 เวอร์ชันต่างกันในโค้ดจริง**: (ก) โมเดลหลัก `Meter.php` ใช้เกณฑ์ `percentFull > 90` พร้อม fallback "มิเตอร์ใหม่", (ข) `reports/meterExcel.php` (`calculateMeterUnits`) ใช้เกณฑ์ `percentFull >= 95` ไม่มี fallback, (ค) Sheet 2 ของทั้งสองไฟล์ export ใช้สูตรง่าย ๆ `units = current` เสมอเมื่อ `meter_reset=1` โดยไม่เช็ค percentFull เลย — ตัวเลขที่ผู้ใช้เห็นในตารางหลัก vs ในรายงาน Excel ที่ export ออกไปอาจไม่ตรงกันสำหรับเคสมิเตอร์รีเซ็ต
6. **`maxMeterValue` เดามาจากขนาดของเลขมิเตอร์เดือนก่อนแบบ hard-coded เป็น step เดียว (100/1000/10000)** — ถ้าเลขมิเตอร์เดือนก่อนน้อยกว่า 100 (เช่นห้องเพิ่งเปิดใหม่ ใช้น้อยมาก) จะยังถูกตีความว่าเป็นมิเตอร์ 4 หลัก (`maxMeterValue=9999` default) ซึ่งอาจไม่ตรงกับความจริง
7. **ตรวจชนิดไฟล์รูปภาพจาก MIME type ที่ browser ส่งมา (`$_FILES[...]['type']`) เท่านั้น ไม่ตรวจ magic byte จริง** — ปลอมแปลงนามสกุล/MIME เพื่ออัปโหลดไฟล์อันตรายได้ค่อนข้างง่าย ต้องเสริมการตรวจสอบใน PHP 8.4 (เช่น `getimagesize()`, `finfo_file()`)
8. **ไฟล์รูปเก่าไม่ถูกลบเมื่อมีการอัปโหลดรูปใหม่ทับ หรือเมื่อผู้ใช้กด "ลบรูป"** — สะสม orphan file ใน `public/uploads/meters/` ไปเรื่อย ๆ ไม่มี cleanup job ปรากฏในโค้ด นอกจากนี้รูปที่อัปโหลดผ่าน `uploadTempImage` ระหว่าง import Excel แล้วผู้ใช้ปิด modal โดยไม่กด "บันทึก" ก็จะกลายเป็น orphan file เช่นกัน (ไฟล์ถูกเขียนลงดิสก์ทันทีที่เลือก ไม่ใช่ตอนบันทึกจริง)
9. **`chkNumber()` (จำกัดกรอกเฉพาะตัวเลขที่ช่องมิเตอร์) ป้องกันไม่สมบูรณ์** — ดักได้แค่ event `keypress` เท่านั้น ยังสามารถ paste ข้อความที่ไม่ใช่ตัวเลขเข้าไปได้ และฟิลด์ HTML เป็น `type="number"` (บาง browser ยอมให้พิมพ์ `e`, `+`, `-`, `.` ได้ตามสเปก HTML5) — backend รับด้วย `(int)` cast เฉย ๆ (ค่าที่ cast ไม่ได้จะกลายเป็น 0 เงียบ ๆ ไม่ error)
10. **ค่ามิเตอร์/ค่าใช้จ่ายที่ backend รับเป็น `(int)`/`(float)` cast ไม่มีการตรวจช่วงค่าเลย** — ไม่ป้องกันค่าติดลบตอนบันทึก raw (`electricity`, `water`, `garbage`, `common_area` ติดลบได้ในระดับ storage แม้ว่าตอนคำนวณ "units" จะถูก floor ที่ 0) เช่น ถ้ากรอกเลขมิเตอร์ปัจจุบันเป็นค่าติดลบ ระบบจะเก็บค่านั้นตรง ๆ ใน `reading_value`
11. **การ validate "เลขมิเตอร์ต้อง ≥ เดือนก่อน" มีเฉพาะฝั่ง client ของ Import Excel เท่านั้น** — ทั้งฟอร์มเดี่ยว (`saveMeter`) และ backend ของ bulk import (`bulkSaveFromExcel`) ไม่ตรวจซ้ำเลย ผู้ใช้สามารถบันทึกเลขมิเตอร์ปัจจุบันน้อยกว่าเดือนก่อนผ่านฟอร์มเดี่ยวได้ (โดยไม่ติ๊ก reset/changed) ผลคือ `units` จะถูกคำนวณติดลบแล้วถูก floor เป็น 0 อัตโนมัติ (เงียบ ๆ ไม่มี warning ให้ผู้ใช้เห็น มีแค่ `error_log` ฝั่ง server)
12. **มิเตอร์ใหม่ที่ยังไม่เคยมีข้อมูลเดือนก่อน (`previous = 0`)**: หน่วยที่คำนวณได้ = `current - 0 = current` ทั้งหมด — ถ้าห้องเพิ่งเปิดใช้งานกลางเดือน เลขมิเตอร์ตัวแรกที่จดได้ (ซึ่งอาจไม่ใช่ 0) จะถูกคิดเป็นหน่วยใช้ทั้งหมดในงวดแรกทันที (ไม่มี field แยกสำหรับ "เลขมิเตอร์เริ่มต้นตอนติดตั้ง" ที่ต่างจาก reading ปกติ)
13. **สถานะ `saved` ไม่ได้แปลว่ามีค่าที่มีความหมาย** — แถวที่มี `reading_value = NULL` ทั้งไฟและน้ำ (เช่นกดบันทึกฟอร์มว่างเปล่าไว้ก่อน) จะยังถูกนับเป็น `'saved'` เพราะมีแถวถูก insert ไว้ใน `me_meter` แล้ว (ดูหัวข้อ 8.1)
14. **`me_meter_ohter` (ขยะ+ส่วนกลาง) ไม่ถูกลบแบบ transactional เมื่อ delete มิเตอร์** — ถ้าลบ `me_meter_ohter` ล้มเหลว (catch แล้ว log เฉย ๆ) แต่ `me_meter` ลบสำเร็จ จะเหลือ `me_meter_ohter` เป็นข้อมูลกำพร้า (orphan) ของ pcode/month/year ที่ไม่มีมิเตอร์คู่กันแล้ว ซึ่งจะยังทำให้สถานะของงวดนั้นถูกมองว่า `'saved'` ต่อไปแม้ลบมิเตอร์ไปแล้ว (เพราะ `hasMeterDataBatch` เช็ค `me_meter_ohter` ด้วย)
15. **ไฟล์ `views/pages/meterExcel.php` (ไม่มี `/reports/`) เป็นไฟล์ที่ไม่ถูกอ้างอิงจากที่ใดในระบบเลย** (ตรวจสอบด้วย grep ทั้งโปรเจกต์แล้ว พบการอ้างอิงเฉพาะใน `views/pages/meter-management/index.php` ที่ชี้ไปยัง `/views/pages/reports/meterExcel.php` เท่านั้น) — เป็นไฟล์ orphan/legacy ที่อาจสร้างความสับสนได้ถ้า migrate ยกทั้งโฟลเดอร์ ควรตัดทิ้งหรือ archive แยกไว้ ไม่ใช่ migrate เป็นฟีเจอร์จริง
16. **`reports/meterExcel.php` (รายงานตามช่วงเดือน) ดึงค่าขยะ/ค่าส่วนกลางจาก `me_meter` ด้วย `meter_type IN ('ค่าขยะ','ค่าส่วนกลาง')` — แต่ในความเป็นจริงค่าเหล่านี้ถูกเก็บอยู่ในตาราง `me_meter_ohter` (คอลัมน์ `meter_ohter_type`) ไม่ใช่ `me_meter`** ดังนั้น query นี้จะไม่พบแถวใด ๆ เลยเสมอ ทำให้คอลัมน์ "ค่าขยะ" และ "ค่าส่วนกลาง" ในรายงาน Excel Sheet 1 **เป็น 0 เสมอ ไม่ว่าจะมีการบันทึกจริงหรือไม่** — เป็นบั๊กที่ยืนยันจากโค้ดจริง (ดูหัวข้อ 6 ของไฟล์ต้นฉบับ บรรทัด ~197-230) ต้องแก้ไขให้ query จาก `me_meter_ohter` แทนในระบบใหม่
17. **`meterExcel.php` เวอร์ชันเก่า (ไม่ใช่ `/reports/`) คำนวณค่าขยะ/ส่วนกลางจาก `me_invoice`** (คนละ logic กับทั้งสองข้อข้างบน) — ยืนยันว่าทั้ง 3 ไฟล์ที่เกี่ยวกับ export/รายงานมิเตอร์มีวิธีดึงค่าขยะ/ส่วนกลางที่**ไม่ตรงกันเลยสักไฟล์** เป็นสัญญาณว่าฟีเจอร์นี้ผ่านการแก้ไขหลายรอบโดยไม่ synchronize logic — ระบบใหม่ควรมีจุดคำนวณเดียว (single source of truth)
18. **`updateLatestMeterValues()` เขียนทับ `ali_product.meter_1_latest`/`meter_0_latest` เฉพาะเมื่อค่าที่ส่งมา `> 0`** — ถ้าผู้ใช้บันทึกเลขมิเตอร์เป็น 0 โดยตั้งใจ (เช่นมิเตอร์ใหม่จริง ๆ เริ่มจาก 0) ค่าแคชนี้จะไม่อัปเดตตาม (ยังคงค่าที่เคยแคชไว้ก่อนหน้า) — เป็น edge case ที่ควรพิจารณาว่าใช่พฤติกรรมที่ต้องการหรือไม่
19. **`updateCurrentCosts()` เขียนทับ `current_waste`/`current_central` ทุกครั้งที่บันทึก (ถ้ามีค่าใดค่าหนึ่งไม่ null)** แม้ในโหมด "แก้ไข" ก็ตาม กลายเป็นว่าค่า default ของห้องนั้นจะเปลี่ยนตามค่าล่าสุดที่เคยกรอกเสมอ ไม่ใช่ค่าคงที่ประจำห้อง — มีผลกับ auto-fill ของงวดถัดไป (หัวข้อ 6.1 ข้อ 9 และ 4.4 ส่วน auto-fill)
20. **การ generate หมายเหตุอัตโนมัติจะเขียนทับข้อความที่ผู้ใช้แก้ไขเองทุกครั้งที่เปลี่ยนวันที่อ่านมิเตอร์** (ผ่าน `change` event แบบ real-time ในหัวข้อ 4.4) — ถ้าผู้ใช้พิมพ์หมายเหตุเพิ่มเติมเอง (เช่น "มิเตอร์อยู่หลังบ้าน เข้ายาก") แล้วบังเอิญแก้วันที่อีกครั้ง ข้อความที่พิมพ์เองจะหายไปทันทีโดยไม่มีการยืนยัน
21. **Import Excel: การ validate ช่วงวันที่ (1 ของเดือนก่อน ถึงวันสุดท้ายของเดือนที่เลือก) คำนวณด้วย JavaScript `Date` object ฝั่ง client ล้วน ๆ** ขึ้นกับ timezone ของเบราว์เซอร์ผู้ใช้งาน อาจมีพฤติกรรมคลาดเคลื่อนที่ขอบเขต (เช่น timezone ที่ไม่ใช่ UTC+7) เมื่อเทียบวันที่ใกล้เที่ยงคืน
22. **Progress bar ระหว่าง bulk save เป็นค่าจำลอง (fake progress) ไม่ใช่ progress จริงจาก backend** — request เดียวส่งข้อมูลทั้งหมดพร้อมกันเป็น JSON array เดียว ระบบไม่ได้ stream ผลลัพธ์ทีละแถวกลับมา ถ้าจำนวนแถวเยอะมากอาจใช้เวลารอนานโดยที่ progress bar แสดงค้างที่ ~88% เป็นเวลานาน

---

## 11. ความสัมพันธ์กับเมนูอื่น

### 11.1 โครงการ/สินค้า (Product Management, `ali_product`)

- ข้อมูลห้อง/ยูนิตทั้งหมดที่แสดงในตารางมิเตอร์ **มาจากตาราง `ali_product` โดยตรง** ผ่าน join กับ `ali_productcategory`/`ali_productgroup` — ถ้าห้องใดยังไม่ถูกสร้างในเมนูสินค้า (หรือ `sh != 1`, หรือหมวดหมู่ไม่ใช่ 34/54) จะ**ไม่ปรากฏในรายการมิเตอร์เลย**
- `meter_1_ppu`/`meter_0_ppu` (ราคาต่อหน่วยไฟ/น้ำ) ต้องถูกกำหนดไว้ที่ระดับสินค้า — ถ้าไม่มีค่า จะ `COALESCE` เป็น 0 (ทำให้คำนวณค่าไฟ/น้ำได้ 0 เสมอแม้มีหน่วยใช้จริง)
- `meter_1_latest`/`meter_0_latest` และ `current_waste`/`current_central` ใน `ali_product` เป็น **แคชที่เขียนกลับ (write-back cache)** จากการบันทึกมิเตอร์ทุกครั้ง — ทำให้ฟีเจอร์ "บันทึกมิเตอร์" มีผลข้างเคียงเขียนข้อมูลกลับไปยังตาราง Product โดยตรง (ไม่ใช่แค่อ่านอย่างเดียว) ต้องระวังตอนออกแบบ ownership ของข้อมูลในระบบใหม่
- `wh_st` (สถานะห้อง: ว่าง/ไม่ว่าง/จอง/โครงการในอนาคต) ถูกดึงมาแสดงเป็นข้อความในหัวข้อ modal เท่านั้น (`(สถานะ{wh_st_status})`) — ไม่มีผลต่อ business logic การบันทึกมิเตอร์ (บันทึกได้แม้ห้องว่าง)
- `sales_rep_code` ใช้กรองข้อมูลตาม role `agent` (พนักงานขาย) — ถ้า login ด้วย role `agent` จะเห็นเฉพาะห้องที่ `sales_rep_code` ตรงกับ `code` ของตนเองเท่านั้น (บังคับใน `getAllMeters()`)

### 11.2 ใบแจ้งหนี้ (Invoice Management, `me_invoice`)

- ความสัมพันธ์ทางเดียวที่สำคัญที่สุด: **มีใบแจ้งหนี้แล้ว → ข้อมูลมิเตอร์ของงวดนั้นถูกล็อกไม่ให้แก้ไข/ลบ** (บังคับใน UI เสมอ, บังคับที่ backend เฉพาะ `delete()` และ `bulkSaveFromExcel()` แต่ **ไม่บังคับที่ backend ของ `saveMeter()`** — ดู edge case ข้อ 3)
- ทุกครั้งที่โหลดตาราง (`getByPeriod`) จะยิง query เช็ค `me_invoice` **แยกทีละแถว (N+1 query pattern)** ไม่ได้ join หรือ batch เหมือนส่วนอื่นของโมเดล — เป็นจุดที่ประสิทธิภาพต่ำกว่าที่ควรเมื่อมีจำนวนห้องมาก ควรแก้เป็น batch query ในระบบใหม่
- ข้อมูลค่าขยะ/ส่วนกลางที่ผูกกับ invoice (ในไฟล์ `meterExcel.php` เวอร์ชันเก่าที่ไม่ได้ใช้งานจริง) แสดงให้เห็นว่าที่มาของค่าขยะ/ส่วนกลางอาจถูกดึงจาก invoice ได้ในบาง flow เก่า แต่ flow ปัจจุบัน (การบันทึกมิเตอร์) เก็บค่าเองอิสระในตาราง `me_meter_ohter` ไม่ได้ผูกกับ invoice โดยตรง — ใบแจ้งหนี้ที่ถูกสร้างขึ้นภายหลังน่าจะเป็นผู้**อ่าน**ค่าจาก `me_meter`/`me_meter_ohter` ไปสร้างรายการ (เป็นเรื่องของโมดูล Invoice ซึ่งอยู่นอกสโคปเอกสารนี้)

### 11.3 ผู้ใช้งาน/สิทธิ์ (`me_users`, `Auth`)

- ทุก endpoint ต้องผ่าน `Auth::check()` (เช็คใน constructor ของ controller) มิฉะนั้น redirect ไป `/login`
- `Auth::user()['role']` ใช้กรองสิทธิ์การมองเห็นห้อง (`agent` เห็นเฉพาะห้องของตน ตามหัวข้อ 11.1)
- `Auth::user()['id']` ถูกบันทึกเป็น `created_by`/`updated_by` ในทุกการ insert/update ของ `me_meter` และ `me_meter_ohter`
- `me_users.name` ใช้แสดงผล "บันทึกล่าสุดโดยใคร" ใน modal แก้ไข (ผ่าน `getLastUpdatedInfo`)

---

*จบเอกสาร — ครอบคลุมทุกฟังก์ชัน, endpoint, สูตรคำนวณ, และเงื่อนไข business logic ที่พบในซอร์สโค้ดจริงของฟีเจอร์ "บันทึกมิเตอร์" ณ วันที่จัดทำเอกสาร (อ้างอิงจากโค้ดล่าสุดในสาขา `dev`)*
